# Creating a Recursica Adapter

This document is the repeatable process for turning a third-party React UI kit into a Recursica adapter: a standalone, independently-published npm package that lets application developers use that UI kit through Recursica's unified component API and design tokens.

It's written so anyone — human or AI agent — can follow it for a **new** UI kit, in a **new**, empty repository. It doesn't assume access to any particular person's machine; the only adapter it references is **the genesis adapter, `mantine-adapter`** — the original, most complete, actively-maintained Recursica adapter, published in the public `recursica` monorepo (`https://github.com/borderux/recursica/tree/main/packages/mantine-adapter`). Every other adapter, including this one, is built by following this document against that single reference.

## 0. Prerequisites

- A UI kit that ships React components and can be installed as an npm peer dependency.
- Read access to the `recursica` monorepo (`https://github.com/borderux/recursica`), which hosts:
  - **The genesis adapter**: `packages/mantine-adapter`. Use it as the structural reference for every step below.
  - **`packages/adapter-common`** (published as `@recursica/adapter-common`) — the framework-agnostic shared layer every adapter depends on: prop-contract types (`RecursicaOverStyled`, `RecursicaSpacing`, etc.), `RecursicaThemeProvider`, `Layer`, and the **canonical** component/Storybook dev guides.
- A new, empty GitHub repository for the adapter (e.g. `recursica-<ui-kit>-adapter`), created under whichever org will own it.
- Node.js >=20, npm.

## 1. Understand the genesis adapter before copying anything

Read, in order:

1. `packages/adapter-common/src/types.ts` and `packages/adapter-common/src/index.ts` in the monorepo — this is the actual contract an adapter implements (`RecursicaOverStyled<T>`, `RECURSICA_COMPONENTS`/`RecursicaComponent`, `RecursicaThemeProvider`).
2. `packages/adapter-common/docs/PIPELINE.md` — how doc and code changes flow from `adapter-common` through an adapter to Storybook.
3. `packages/adapter-common/docs/COMPONENT_DEV_GUIDE.md` and `docs/COMPONENT_STORYBOOK_GUIDE.md` — the canonical rules every adapter's components must follow.
4. `packages/mantine-adapter/`'s own root docs (`README.md`, `ARCHITECTURE.md`, `SETUP.md`, `CONTRIBUTING.md`, `USAGE.md`, `OVERSTYLING.md`, `AGENT.md`, `docs/PHILOSOPHY.md`) and its `package.json`, `tsconfig.json`, `eslint.config.mjs`, `vite.config.ts`, `.storybook/`.

Do not skip straight to copying files — several things in the genesis adapter are monorepo-specific and must **not** be replicated as-is in a standalone repo (see step 3).

## 2. Write `docs/ADAPTER_INTEGRATION_REPORT.md`

Before scaffolding anything, produce a written reconnaissance report answering, with real evidence from the underlying UI kit's actual source/compiled output (not its marketing docs, not guessed from component names) — not a scaffold-time formality, this is what step 9's stub order and step 10's per-component build order are actually planned from:

1. **How does the underlying kit prefer to style components?** (Plain CSS? CSS-in-JS? CSS custom properties? Does importing a component side-effect-import its styles, or is there a separate stylesheet step?) This seeds — but doesn't replace — step 8's `docs/STYLING_SYSTEM.md`, which is the authoritative, fully-detailed decision record once implementation starts; this step's answer can and should be shorter, since its job is orientation, not the final decision.
2. **How are the underlying kit's own styles included** (imports, providers, stylesheet order)? Write or update `SETUP.md`/`USAGE.md` with the actual, verified steps to wire up the underlying kit's imports/providers/stylesheets alongside this adapter's own — don't leave this to be rediscovered per-component.
3. **Can CSS Modules override the underlying kit's styles without touching global/underlying-kit styles?** (I.e., is step 8's job even achievable with this UI kit — most will be yes, but confirm rather than assume.)
4. **Can appearance-affecting styling props be filtered out to prevent overstyling, and which ones?** A first pass — the per-component `UNSUPPORTED_PROPS` audit (step 10, item 1) is where this gets nailed down exactly, but a rough survey here (does the kit have a generic `sx`/`css`/`classes`-style escape hatch beyond plain `className`/`style`? any components with unusually large styling-prop surfaces?) shapes expectations going in.
5. **Do the underlying kit's own primitives map to Recursica's layout components** (`Flex`, `Stack`, `Group`, `Grid`)? If the kit has no equivalent, that's a real gap to flag now, not discover mid-build.
6. **Does the underlying kit have generic typography primitives** like Recursica's `Text`/`Title`? Same reasoning — some kits (Mantine) have exactly this; others may only have component-embedded text with no standalone primitive.
7. **How does the underlying kit structure form controls** — label placement, description/help-text, error/read-only states, and layout (stacked vs. side-by-side)?
8. **Given (7): can the underlying kit's form controls be styled to match Recursica's stacked/side-by-side layouts directly, or does this adapter need its own `FormControlWrapper`/`FormControlLayout` (mantine-adapter's approach) to get Recursica's look regardless of what the underlying kit does natively?**
9. **A full component-by-component mapping.** For every component the genesis adapter (`mantine-adapter`) implements, identify the underlying kit's closest equivalent (if any — note naming mismatches, e.g. a plural/singular or spelling difference) and categorize it as **Easy** (a near-identical single component), **Requires work** (needs composing multiple primitives, or has real prop/behavior gaps to close), or **Does not exist** (would need substantial new implementation). Turn this into **the numbered build-order list** step 9/10 actually follow — easiest first, hardest last — not a build order invented separately later.
10. **How does the underlying kit handle light/dark mode, how do you switch between them at runtime, and how does that interact with `RecursicaThemeProvider`?** Don't stop at "how is it themed" — actually verify a live toggle keeps both the underlying kit's own theme and Recursica's theme in sync, not just that each independently supports light/dark. A provider's "default"-named theme prop is a common trap: confirm whether it's read reactively on every render or only once at mount (if the latter, runtime switching needs the kit's own hook/context API, not just re-rendering the provider with a new prop value) — and prove it with a real before/after check (e.g. via Playwright against a running Storybook), not by reading the type signature and assuming.

Read the underlying kit's actual `.d.ts`/compiled source for every claim in this document, the same rigor `docs/STYLING_SYSTEM.md` and every component's `IMPLEMENTATION_NOTES.md` already require — a wrong guess here misdirects the entire build order, not just one component. This document is **not published** (not a consumer-facing doc — it's for whoever builds this adapter next, human or AI), so it can be as long and evidence-dense as it needs to be.

## 3. Decide what's monorepo-specific vs. portable

Before scaffolding, identify:

- **Real npm dependencies** the adapter needs (`@recursica/adapter-common`, `@recursica/official-release`, `@recursica/storybook-template`, `@recursica/recursica-postcss-vars`, `@recursica/token-analyzer`, the UI kit itself). Confirm each is actually published (`npm view <package> version`) — don't assume a package referenced with `"*"` in the monorepo's `package.json` is public. Some devDependencies are internal-only build tooling (scoped `@repo/*`) and will 404 on the public registry. Drop those; if a tool that 404s turns out to be load-bearing, that's an open item to raise with whoever owns the monorepo, not something to silently work around — see the "initial scaffold" and "`@recursica/token-analyzer` brought in" decisions-log entries below for exactly this happening with token-analyzer (dropped when unpublished, brought back in once it was published).
- **Docs that ship in the npm package** vs. **contributor-only docs**. Check the genesis adapter's `package.json` `"files"` array — only what's listed there reaches consumers. Everything else (`CONTRIBUTING.md`, `AGENT.md`, the canonical `adapter-common/docs/*` guides) lives in the repo but isn't published, so a standalone adapter's docs must link to them as **absolute GitHub URLs** back into the monorepo, not relative paths — they don't exist in the published package or in a repo that doesn't contain them.
- **CI/publishing**: the monorepo's `.github/workflows/release.yml` is a Turborepo + Changesets pipeline that also builds Figma plugins. A standalone single-package repo needs its own, much simpler version: build → Changesets version/publish. No Turborepo, no zip/upload-release-assets steps.
- **Repo-relative doc links** (`../adapter-common/docs/...`) become absolute GitHub URLs once the adapter is out of the monorepo.
- **Git hooks**: the monorepo enforces formatting/linting/type-checking/tests at commit time via Husky + lint-staged (`.husky/`, `scripts/lint-staged.config.js`, root `package.json`'s `prepare`/`precommit` scripts). A standalone repo needs its own copy of this, simplified for a single package (see step 4).

## 4. Scaffold the package

Mirror the genesis adapter's root-level structure inside the new repo (not inside a `packages/` subfolder — the new repo's root _is_ the package root):

```
<repo-root>/
├── .changeset/               # config.json + README.md (standalone: no "linked"/"fixed" cross-package config needed)
├── .github/workflows/        # release.yml, pull-request.yml (standalone versions, see step 7)
├── .husky/                    # install.mjs, pre-commit (git hooks — see below)
├── .storybook/                # main.ts, preview.tsx, manager.ts, RecursicaTheme.ts, commonArgTypes.ts, preview-head.html, vitest.setup.ts
├── docs/
│   ├── PHILOSOPHY.md          # published; adapt Mantine-specific language to the new UI kit
│   ├── COMPONENT_DEV_GUIDE.md # NOT published; thin delta, links to the canonical guide via GitHub URL
│   ├── COMPONENT_STORYBOOK_GUIDE.md # same
│   ├── ADAPTER_INTEGRATION_REPORT.md # NOT published; written in step 2, before scaffolding starts
│   ├── STYLING_SYSTEM.md      # NOT published; written in step 8, before any component CSS
│   └── CREATING_AN_ADAPTER.md # this file, if the new adapter is meant to keep documenting the process further
├── scripts/
│   └── lint-staged.config.cjs # single-package lint-staged config (see below)
├── src/
│   ├── index.ts                # the package's entry point (vite.config.ts's build.lib.entry points here): imports @recursica/adapter-common/style.css, re-exports ./components
│   ├── env.d.ts
│   ├── GlobalExemptions.modules.css
│   ├── types/                  # index.ts, scss-modules.d.ts, plus a UI-kit module-augmentation file if needed
│   ├── utils/                  # index.ts, filterStylingProps.ts (UI-kit-specific BLOCKED_STYLING_KEYS/LAYOUT_PROPS)
│   └── components/             # index.ts barrel: re-exports @recursica/adapter-common + every component; one folder per component, added incrementally
├── AGENT.md
├── ARCHITECTURE.md
├── CHANGELOG.md
├── CONTRIBUTING.md
├── LICENSE
├── OVERSTYLING.md
├── README.md
├── SETUP.md
├── USAGE.md
├── .prettierignore
├── eslint.config.mjs
├── llms.txt
├── package.json
├── prettier.config.mjs
├── recursica.json
├── tsconfig.json
├── vite.config.ts
├── vitest.workspace.ts
└── vitest.shims.d.ts
```

Adapt each config file from the genesis adapter:

- **`package.json`**: rename (`@recursica/<kit>-adapter`), update `description`/`repository`/`homepage`/`bugs` to the new repo (no `directory` field — it's not a monorepo subpackage), replace `peerDependencies` with the new UI kit's packages and versions (check the UI kit's own `peerDependencies` for the React version range it supports), keep `@recursica/adapter-common` + `@recursica/official-release` as real dependencies, pin `"*"` devDependencies to their actual published versions, add `version`/`release` scripts (`changeset version` / `changeset publish`) since there's no monorepo root to define them either. **Do not add a `test` script** (see the "removed the test infrastructure" decisions-log entry below) — the genesis adapter (`mantine-adapter`) doesn't have one either, despite shipping the same `vitest`/`@storybook/addon-vitest`/`playwright` devDependencies; match that, don't go beyond it, unless a human explicitly asks for automated story tests. `check-types` is a real gap worth asking about explicitly before adding — the genesis adapter doesn't have that script either, and it's not yet resolved here whether that's intentional or an oversight in the genesis adapter itself. Pick an unused Storybook port relative to the genesis adapter's own (`6011`).
- **`tsconfig.json`**, **`eslint.config.mjs`**: copy as-is — both were already self-contained in the genesis adapter (neither actually extends the internal `@repo/typescript-config`/`@repo/eslint-config` packages despite them appearing in `devDependencies`).
- **`vite.config.ts`**: update the library `name`/`fileName` and the `rollupOptions.external` list to the new UI kit's package name(s).
- **`.storybook/main.ts`**, **`manager.ts`**, **`RecursicaTheme.ts`**, **`commonArgTypes.ts`**, **`preview-head.html`**, **`vitest.setup.ts`**: copy as-is — these use `@recursica/storybook-template` and are UI-kit-agnostic.
- **`.storybook/preview.tsx`**: replace the UI kit's provider/theme wrapper (in mantine-adapter, `<MantineProvider>` + `ColorSchemeWrapper`) with the new UI kit's equivalent. If you don't yet know the UI kit's theming API with confidence, leave a clearly-marked `TODO` rather than guessing — an unverified provider wrapper is worse than a documented gap.
- **`.changeset/config.json`**: copy as-is (single-package config needs no `linked`/`fixed` package groups).
- **`.github/workflows/`**: strip Turborepo caching, figma-plugin env vars, and the zip/upload-release-assets steps. Keep: checkout → setup Node → `npm ci` → `npm run check-types`/`npm run lint`/`npm run build` (PR workflow) and checkout → setup Node → `npm ci` → build → `changesets/action@v1` with `publish: npm run release` (release workflow). No `npm run test` step — see the note on not adding a `test` script above.
- **Git hooks (Husky + lint-staged)**: the monorepo's root `package.json` wires a `prepare` script (`node .husky/install.mjs`) that installs Husky on `npm install`, and a `.husky/pre-commit` hook that runs `npx lint-staged --config scripts/lint-staged.config.js`. Copy `.husky/install.mjs` as-is. For `.husky/pre-commit`, copy the one-line command but write it as `npx lint-staged --config scripts/lint-staged.config.cjs` — **`.cjs`, not `.js`**: the monorepo's root `package.json` has no `"type"` field (defaults to CommonJS), but a standalone adapter's `package.json` has `"type": "module"` (needed for the rest of the ESM tooling), and lint-staged's config uses CommonJS `module.exports`. A plain `.js` file gets parsed as ESM under `"type": "module"` and fails to load — `.cjs` is unambiguous regardless of the package's `"type"`. `scripts/lint-staged.config.cjs` also needs to be **simplified**: the monorepo's version routes staged files to their owning `apps/*`/`packages/*` directory and runs `turbo run check-types test --filter=...` per affected package — there's no Turborepo and only one package here, so the standalone version just runs `prettier --write`/`eslint --fix` on staged JS/TS files plus `npm run check-types` on the whole package (no `test` step — see the note on not adding a `test` script above; see this repo's `scripts/lint-staged.config.cjs` for the concrete version). Add `husky`, `lint-staged`, and `prettier` as devDependencies (pin to the same versions the monorepo uses), add a `format` script (`prettier --write "**/*.{js,jsx,ts,tsx,json,md,css,scss}"`), a `precommit` script (`npm run check-types && npm run lint && npm run format`, matching the monorepo's), and a root `prettier.config.mjs` (empty is fine — the monorepo's is empty too, meaning default Prettier settings) plus a `.prettierignore` that at minimum excludes `dist`, `storybook-static`, and the `recursica_*`/`recursica.json` files `@recursica/official-release`'s postinstall manages (see step 5's note on that postinstall behavior — reformatting those files fights it). Run `npm install` afterward so the `prepare` script actually installs the hook, then confirm it took with `git config --get core.hooksPath` (should print `.husky/_` or equivalent, not be empty) — and actually try a commit (or run `npx lint-staged --config scripts/lint-staged.config.cjs` directly) rather than assuming the hook works, since a broken config here silently blocks every future commit.

## 5. Scaffold `src/`

Even before any real component exists, `src/` should be a **buildable, empty adapter**:

- `src/index.ts` is the package's entry point (`vite.config.ts`'s `build.lib.entry` points here): imports `@recursica/adapter-common/style.css` and re-exports `./components`.
- `src/components/index.ts` re-exports everything from `@recursica/adapter-common` and starts as an otherwise-empty barrel.
- `src/utils/filterStylingProps.ts` re-exports the shared types/utils from `@recursica/adapter-common` and defines `BLOCKED_STYLING_KEYS` for the new UI kit. Start this list from what the UI kit's components actually accept as styling props (Mantine exposes a large atomic style-prop system; not every UI kit does — check the UI kit's own component prop docs rather than copying Mantine's list wholesale) and expand it as real components surface gaps. Leave `LAYOUT_PROPS`/`SPACING_MAP` wiring in place (framework-agnostic — it maps `rec-*` tokens to `--recursica_brand_dimensions_*` CSS variables) but empty `LAYOUT_PROPS` until a layout component is built.

Run `npm install`, then `npm run build`, `npm run lint`, `npm run check-types` to confirm the empty scaffold actually compiles and builds before writing any docs content or components on top of it. (`npm install` needs to be run once, by a human, to generate `package-lock.json` — see the open items log.)

**Note on `@recursica/official-release`'s postinstall**: it copies default theme files (`recursica_variables_scoped.css`, `recursica_brand.json`, `recursica_tokens.json`, `recursica_ui-kit.json`, and — easy to miss — `recursica.json` itself) into the project root the first time `npm install` runs and `recursica_variables_scoped.css` doesn't already exist. It does this unconditionally for all five files, including overwriting a hand-authored `recursica.json` with no existence check on that file specifically. Since this only fires once per fresh clone (gated on the CSS file's presence), the practical effect is: **write your intended `recursica.json` content, run `npm install` once, then rewrite `recursica.json` back to what you want.**

**All five files are checked into git, and `@recursica/official-release` is bootstrap-only** — this is the expected workflow, not an accident: initialize the repo by running `npm install` (which triggers this postinstall and populates the root with the current `@recursica/official-release` version's tokens), then commit what it generated. Don't gitignore any of them, and don't have `vite.config.ts`/`.storybook/main.ts`/`.storybook/preview.tsx` resolve their working copies from `node_modules/@recursica/official-release/...` — point them at the checked-in root files instead (`resolve(__dirname, "../recursica_variables_scoped.css")`, `import "../recursica_tokens.json"`, etc.), so what's committed is provably what the build reads. `@recursica/official-release` isn't consulted again after that first install; the root copies are what a developer edits going forward (by hand, or via a Forge Recursica export per `SETUP.md`). Note the postinstall's own guard (see below) means a plain `npm install` after bumping `@recursica/official-release`'s version won't refresh them — `recursica_variables_scoped.css` already existing is exactly what skips the copy — so refreshing deliberately after a version bump means deleting the five files first (or at least `recursica_variables_scoped.css`) and reinstalling, then committing whatever `git diff` shows as the token drift, the same way you'd review a `package-lock.json` diff for dependency drift.

> **Gotcha**: `.storybook/main.ts` runs under Node's native ESM loader (unlike `vite.config.ts`, which Vite's config loader shims `__dirname` into) — a bare `resolve(__dirname, ...)` there throws and Storybook won't boot at all. Reconstruct it explicitly: `const __dirname = dirname(fileURLToPath(import.meta.url));` (same pattern `eslint.config.mjs` already uses). This is exactly the kind of break that stays invisible through `check-types`/`build` and only shows up when you actually run `npm run storybook` — see step 7.

## 6. Scaffold the docs

Copy each genesis-adapter doc and adapt UI-kit-specific language (framework name, peer install commands, code examples that reference the old UI kit's props):

- `README.md`, `ARCHITECTURE.md`, `SETUP.md`, `USAGE.md`, `OVERSTYLING.md`, `AGENT.md`, `CONTRIBUTING.md`, `llms.txt`, `CHANGELOG.md` (start this one as a stub — Changesets will populate it).
- `docs/PHILOSOPHY.md` — despite living in `docs/`, this one is **published** (it's in `package.json`'s `"files"`) and is **not** a thin delta; the genesis adapter's copy is fully written in Mantine-specific language throughout, so it needs a genuine rewrite for the new UI kit's vocabulary, not just a find-and-replace of the framework name.
- `docs/COMPONENT_DEV_GUIDE.md`, `docs/COMPONENT_STORYBOOK_GUIDE.md` — these **are** thin deltas. Link to the canonical versions in `adapter-common` via absolute GitHub URL, and only document what's genuinely different for the new UI kit. It's fine (expected, even) for these to start nearly empty and grow as real components are built.
- Every doc that references "check the genesis adapter's delta doc for parity" should point to `mantine-adapter` in the monorepo via GitHub URL, not a relative path, since there's no shared workspace here.

## 7. Verify the pipeline end-to-end

Before considering the scaffold "done":

1. `npm install` (generates `package-lock.json`, needed for `npm ci` in CI; also installs the Husky git hook via the `prepare` script).
2. `npm run build` — should produce `dist/` with `.js`/`.cjs`/`.d.ts`/`.css` outputs even with zero components.
3. `npm run lint`, `npm run check-types`, `npm run format`.
4. `npm run storybook` — should boot even with an empty component barrel.
5. Push the branch, open a draft PR, confirm `pull-request.yml` goes green.
6. Confirm `release.yml`'s `NPM_CONFIG_PROVENANCE`/OIDC setup and the `RELEASE_PAT` secret are configured in the new repo's GitHub settings before merging anything — first-time publish of a new scoped package can 404/401 via CI-only auth; see the monorepo's `PUBLISHING.md` "First-Time Package Publishing" section for the manual `npm login && npm publish --access public` workaround if that happens.
7. Wire up `@recursica/adapter-tester` (published, `^5.0.0`+ — `npm install --save-dev @recursica/adapter-tester @playwright/test && npx playwright install chromium`) so the new adapter can be diffed against `mantine-adapter` as the visual source of truth, without needing this monorepo checked out. This is a real CLI (`adapter-tester`) reading an optional JSON config — no Playwright config or spec file to hand-author (earlier versions of this step, when 1.x was hand-config-only and 2.x/3.x/4.x used different config shapes, are preserved in the decisions log below for history, but don't follow them — they're obsolete).

   - Add three scripts: `"adapter-tester": "adapter-tester --serve"` (interactive side-by-side dev mode), `"adapter-tester:automated": "adapter-tester"` (the headless own-drift check against this project's own committed `test/golden/` — fast, no network, run this normally), and `"adapter-tester:source-of-truth": "adapter-tester --divergence-only"` (the separate, opt-in divergence check against the source-of-truth adapter's goldens).
   - Leave the adapter's own `storybook` script with **no fixed `-p <port>`** (a plain `storybook dev`) — `adapter-tester` auto-detects the real port from Storybook's own startup output rather than trusting a configured one, since Storybook silently falls back to an OS-assigned port whenever its default/configured one is taken. A hardcoded port is a first-guess hint at best and a source of flaky `webServer` timeouts at worst. **No config file is required at all** for a bare setup — `adapter-tester` reads the project name from `package.json`. Add `adapter-tester.config.json` only to override a default (raise `sourceOfTruthThresholdPixels`, add per-story `stories.<id>.sourceOfTruthThreshold`/`exclude` overrides, etc.) — see the package's README for the full schema.
   - Add `.adapter-tester/`, `playwright-report/`, and `test-results/` to `.gitignore` — all three are regenerated on every run. Commit `test/golden/` (images + `manifest.json`) — that's the own-drift baseline, not a regenerated artifact.
   - **Read `packages/adapter-tester/AGENT.md` in the monorepo before touching thresholds.** It's an explicit, enforced guardrail: AI agents must never modify `goldenThresholdPixels`/`sourceOfTruthThresholdPixels` (the two global thresholds — own-drift stays tight, default 10; cross-library divergence is deliberately much looser, default 3500), and may only add per-story `stories.<id>.sourceOfTruthThreshold`/`goldenThreshold` overrides or `exclude: true` with a human's explicit permission, after documenting exactly why the difference is unfixable. The source-of-truth divergence check is a hard fail as of 5.0 (previously a soft, non-failing flag) — a genuine, reviewed divergence needs `--approve-divergence` (only valid combined with `--divergence-only`), not a threshold bump, to stop re-flagging until the source of truth's own golden changes again. A threshold failure is the tool doing its job, not something to route around — see the decisions log entry below for a case that looked like an unfixable cross-library difference at first glance and turned out to be a real, fixable bug in the _tool's own harness_, not the adapter.
   - `adapter-tester:source-of-truth` also runs a **story parity check**: it fails if `mantine-adapter` has a golden for a story id this adapter's Storybook doesn't have at all (and hasn't marked `exclude: true`). Porting the missing story is almost always the right fix over excluding it.

   **Story titles must match `mantine-adapter`'s `"UI-Kit/<Component>"` convention exactly** — the suite fetches the source of truth's story index and requests the _same story id_ from the target adapter's Storybook. A component titled `"Components/Button"` (this repo's stub-scaffolding default, see step 9) produces story ids like `components-button--default`, not `ui-kit-button--default` — the target-side navigation then hits a nonexistent story, `#storybook-root` never renders, and the test times out rather than failing with a useful diff. **Rename a component's story title to `"UI-Kit/<Component>"` as part of turning it from a stub into a real implementation** (Button and Loader were renamed as part of this integration) — not before, since there's no point making an unimplemented stub diffable.

   **Run it once at scaffold time to confirm the harness itself works, not to expect a clean pass — and don't chase every pixel-diff failure as if it's a component bug.** Verified end-to-end here with Button/Loader (the only two real components at the time): of 15 story comparisons, 4 passed outright, 4 timed out because the source of truth has stories this adapter's don't (`PolymorphicAsLink` — Beam's Button has no polymorphism, dropped by design, see Button/IMPLEMENTATION*NOTES.md; Mantine's `Loader` story \_names* like `StaticBarsLarge` don't match this adapter's `Bars`/`Dots`/`LayerTwoOval` naming) — both expected, not bugs — and 7 exceeded the 3500px threshold. Diagnosed (not fully root-caused) that the 7 failures are likely dominated by the global `<Layer>` decorator's padding/background positioning the button differently within the screenshot viewport, not by an actual Recursica-token mismatch — confirmed both adapters' `preview.tsx` default to the same `withLayer: true`/`layer={0}` global wrapping, but didn't get to fully compare the two rendered ancestor chains side by side before concluding. Treat the diff threshold and the exact story-set as things to calibrate with a human once a component is real, not a hard gate at scaffold time — this is genuinely new to the ecosystem and hasn't been calibrated against a non-Mantine-shaped UI kit yet.

## 8. Decide and document the styling system

Before writing any component CSS — stub or real — research how the underlying UI kit actually styles itself and write `docs/STYLING_SYSTEM.md` answering, explicitly, in writing:

1. **How does the underlying UI kit style its own components?** Read its compiled output (not just its docs) — plain CSS classes? CSS custom properties? CSS-in-JS injected at runtime? Does it ship its own stylesheet as a side effect of importing the component, or does the consumer own everything? This determines what override mechanism is even available.
2. **No global style pollution.** A Recursica component must be able to sit next to a plain, undecorated instance of the underlying UI kit's own component with zero bleed either direction. Recursica's overrides must never touch global selectors (`:root`, bare element selectors, the underlying kit's own base classes unscoped) — every override has to be scoped to the specific component instance.
3. **Recursica's own styling should not be trivially targetable/overridable from outside.** Prefer a mechanism that generates unique, non-guessable class names per build (CSS Modules is the default choice for this reason — see `docs/COMPONENT_DEV_GUIDE.md`'s and `CONTRIBUTING.md`'s CSS Modules requirement) rather than fixed, easily-guessed class names another stylesheet could target and override.
4. **Prefer the underlying kit's own idiomatic approach, but prefer static/non-dynamic styling over CSS-in-JS** when there's a choice — deterministic output, no runtime style-computation cost. If the underlying kit is itself CSS-in-JS-only, document that tension explicitly rather than silently picking one side.
5. **Conclude with the concrete overstyling-prevention mechanism** this adapter uses: which override technique (e.g. CSS custom-property overrides vs. direct property overrides — whichever fits the underlying kit's own architecture per point 1) is preferred going forward, and how it composes with the TypeScript (`RecursicaOverStyled`) and runtime (`filterStylingProps`/`omitUnsupportedProps`) blocking layers from `src/utils/filterStylingProps.ts`. Update `docs/STYLING_SYSTEM.md` if a later component reveals the initial conclusion was wrong or incomplete — it's a living doc, not a one-time write.

This only needs the scaffold, not a full component — do it once the pipeline is verified (step 7) and before stubbing/building anything (steps 9–10), since it directly shapes both.

## 9. Stub every component first (recommended)

Before building real components one at a time, generate a placeholder for the full component set — informed by step 2's `docs/ADAPTER_INTEGRATION_REPORT.md`, not independent of it — so the whole surface is importable, type-checked, browsable in Storybook, and already carries the report's per-component findings from day one. This turns "build a component" into "fill in one already-scaffolded file with its own notes already written," not "also wire up scaffolding and re-derive the underlying-kit research as you go."

For each component in the genesis adapter's (`mantine-adapter`) `src/components/` (excluding `RecursicaThemeProvider` and `Layer` — see step 5, those are already covered via `@recursica/adapter-common`'s re-export; also excluding sub-components like `CheckboxGroup`/`AccordionItem`/etc. — stub those alongside their parent when it's actually built):

1. Create `src/components/<Name>/<Name>.tsx` exporting a real, component-specific `<Name>Props` type built on `RecursicaOverStyled` (from `src/utils/filterStylingProps.ts`) — even though the body doesn't use the props yet, the public type shape should already be correct-ish.
2. The body should render a placeholder, not silently return `null` — a missing component should be **obviously** missing. Put the placeholder markup/text in one shared internal component (e.g. `src/components/_InDevelopmentStub.tsx`, leading underscore = not part of the public set) that every stub imports, so the placeholder only needs to change in one place.
3. Create `src/components/<Name>/<Name>.stories.tsx` (CSF3) with `title: "Components/🚧 <Name>"` — **the 🚧 emoji prefix on the story title is what makes in-development components visually obvious in the Storybook sidebar**, grouped and sorted alongside real ones with no extra tooling. Storybook renders titles as-is, so a leading emoji is enough; no addon or custom sidebar renderer needed. Prefix the leaf component name specifically (`Components/🚧 Button`), not the whole path, so the group itself isn't marked.
4. Also create `src/components/<Name>/<Name>.module.css`, **left empty** (a header comment only — no rules yet), and import it into `<Name>.tsx` as `import styles from "./<Name>.module.css";`, applying `className={styles.root}` to the stub's root element. This isn't decorative: it wires up CSS Modules — the adapter's only styling mechanism (see `CONTRIBUTING.md`, `docs/COMPONENT_DEV_GUIDE.md`) — from day one, establishing the `.root`-scoped-class convention every real implementation will build on, and gives every component a working (if empty) style hook to verify against instead of retrofitting one later. An empty `.module.css` compiles fine and `styles.root` resolving to `undefined` at runtime is harmless (`className={undefined}` renders no class).
5. **Create `src/components/<Name>/IMPLEMENTATION_NOTES.md`, seeded from `docs/ADAPTER_INTEGRATION_REPORT.md`'s §9 mapping table** — pull this component's row (category, Beam candidate(s), the specific gap/finding noted) into its own file, plus anything else from the report specifically relevant to it (a cross-reference to a related component's own gap, a downstream note about another story that depends on this one existing, etc.). The goal: whoever picks this component up for step 10 starts from the report's already-verified findings, not a blank page or a re-read of a 170-line document to find their one row. Explicitly flag it as a pre-implementation survey, not a substitute for step 10's own prop audit — re-verification against the underlying kit's real `.d.ts` at implementation time still stands.
6. Export it from `src/components/index.ts` (see step 5's note — `src/index.ts` re-exports `./components` as a whole, nothing to add there per component). If the exported `Recursica<Name>Props` collides with `@recursica/adapter-common`'s own type of the same name, add an explicit `export type { Recursica<Name>Props } from "./<Name>/<Name>";` line right after the `export *` for that component to resolve the ambiguity in favor of the component's own (richer) version, matching the genesis adapter's pattern.
7. **When the component is later implemented for real** (step 10 below), change its story's `title` from `"Components/🚧 <Name>"` to `"UI-Kit/<Name>"` — matching `mantine-adapter`'s own convention exactly, not just stripping the emoji — and stop importing `_InDevelopmentStub`. This isn't cosmetic: `@recursica/adapter-tester`'s visual-regression comparison (step 7, item 7) matches stories by id between the source-of-truth and target Storybooks, and a `"Components/*"`-titled story produces a different id than Mantine's `"UI-Kit/*"` one, so it silently never gets compared (see step 7's adapter-tester entry for what that failure actually looks like). Both the title and the stub-import are signals, not decoration — leaving either on a finished component is misleading. Keep the `.module.css` file and `styles.root` wiring — that part becomes real, not stubbed. The stub's `IMPLEMENTATION_NOTES.md` becomes the real one — expand it with what implementation actually found, don't discard and start over.

This can be done as one batch (49 components at once, in this repo's case) via a small generator script rather than by hand — see the "2026-08-26 — stub components" entry in the decisions log below for exactly what that looked like here, including one required follow-up fix (`eslint`'s `no-unused-vars` needing an `argsIgnorePattern` for the intentionally-unused stub props parameter). That entry also covers a `test` script + CI-testing setup that was tried and then explicitly reverted a few passes later — see the "removed the test infrastructure" entry further down for why; don't re-add it without being asked.

## 10. Build components incrementally

Build components one at a time (whether starting from a step-9 stub or from scratch) following `docs/COMPONENT_DEV_GUIDE.md` (which points to the canonical guide). For each component:

1. **Audit the underlying UI-kit component's full prop list against the canonical `Recursica<Name>Props` contract** (`@recursica/adapter-common`) before writing any implementation code. For every prop the underlying component exposes:
   - **Appearance-affecting props** (color, variant/kind, size, radius, shadow, or anything else that changes how the component looks) that aren't part of the Recursica contract get **removed**: `Omit<>`'d from the public TypeScript type, hardcoded to a sensible default in the render, _and_ added to a local `UNSUPPORTED_PROPS` const passed through `omitUnsupportedProps` — all three, not just the type-level `Omit<>`. The runtime deletion matters even though the component's own render also re-asserts its value after `{...sanitizedProps}` in JSX (so a leaked prop would be overridden by JSX order anyway) — relying on assertion order as the _only_ protection is a real bug class (see `Button/IMPLEMENTATION_NOTES.md`'s "className overwrite bug" citation), so delete explicitly rather than trust order alone.
   - **Behavior-affecting props** with no Recursica equivalent (not about appearance — e.g. a progress value, a controlled/uncontrolled toggle) get a considered, _documented_ decision each: either genuinely useful and passed through as-is, or blocked via the same `UNSUPPORTED_PROPS` mechanism as unsupported. Don't leave a prop unaddressed by default — every prop on the underlying component should have a traceable decision, even if that decision is "allowed through, no Recursica equivalent needed."
   - **Recursica's own props take precedence.** Where the canonical contract defines a prop that maps to (or conflicts with) one of the underlying kit's own props (e.g. Recursica's `variant` mapping to the UI kit's own `variant`/`kind`/`type`-ish prop), the underlying kit's raw prop name is part of the "appearance-affecting, remove it" bucket above — callers only ever get to set the Recursica name, mapped internally to whatever value the underlying kit expects.
2. Implement `ComponentName.tsx` + `ComponentName.module.css`, wrapping the UI kit's component, filtering props through `filterStylingProps`, using CSS variables from `recursica_variables_scoped.css` only (no hardcoded values, no `var(--x, fallback)`). If replacing a step-9 stub, delete the `_InDevelopmentStub` import.
3. Write `ComponentName.stories.tsx` (CSF3) per `docs/COMPONENT_STORYBOOK_GUIDE.md`. If replacing a step-9 stub, remove the `🚧 ` prefix from the story's `title`.
4. Write `USAGE.md` (public) and expand `IMPLEMENTATION_NOTES.md` (internal, not `{Name}_IMPLEMENTATION_NOTES.md` — just `IMPLEMENTATION_NOTES.md`, see any real component's folder) in the component's folder. If starting from a step-9 stub, this file already exists, seeded from the integration report — add what implementation actually found rather than replacing it wholesale.
5. Export it from `src/components/index.ts` (see step 5's note); add the explicit `export type { Recursica<Name>Props }` override alongside it if the name collides with `@recursica/adapter-common`'s own, same as other components.
6. Add it to `llms.txt` in alphabetical order.
7. Add a changeset (`npx changeset`).
8. Update `docs/COMPONENT_DEV_GUIDE.md`/`docs/COMPONENT_STORYBOOK_GUIDE.md` with anything UI-kit-specific you learned building it.

**Verify step 1 actually holds, don't just read the code back**: for at least one appearance-affecting prop you blocked, try to force it through anyway (e.g. via a Storybook story's `args` — `?args=<beamProp>:<value>`) and confirm via a real computed-style/class check that the forced value had no effect. Reading `UNSUPPORTED_PROPS` and confirming the prop name is spelled correctly is not the same as confirming the deletion actually prevents the override at runtime.

## 11. Governance: branches, not commits

Whoever (human or agent) does this work commits to their own feature branch and opens a PR. Committing directly, or merging without human review, is out of scope for an automated contributor — the repo owner reviews and merges.

---

## Decisions & open items log

Dated entries for choices made building **`beam-adapter`** specifically, kept here so the next contributor to this repo knows what's settled vs. still open.

### 2026-08-29 — Storybook published to GitHub Pages, with PR previews (following `recursica-sandbox`'s own pattern)

Matt asked for this repo's Storybook to be published to GitHub Pages on every merge to `main`, plus a PR-preview deploy matching `recursica-sandbox`'s existing setup (`/Users/mattmassey/work/recursica-sandbox/.github/workflows/deploy-main.yml`/`deploy-pr-preview.yml`). Ported both workflows, adapted for this being a single-package library repo with no separate "app" to build:

- **`.github/workflows/deploy-storybook.yml`** (new): on push to `main`, `npm run build-storybook` (already an existing script, outputs to `storybook-static/`) then `JamesIves/github-pages-deploy-action@v4` pushes that folder straight to the `gh-pages` branch **root** — unlike `recursica-sandbox`, which deploys a full app at the root plus Storybook nested under `/storybook` (`dist/` holding both), there's no main app here, so Storybook itself is the whole published site. `clean: false` is carried over from the sandbox's own config specifically so this doesn't wipe out the `pr-preview/` subdirectories the preview workflow below writes into the same branch.
- **`.github/workflows/deploy-pr-preview.yml`** (new): near-verbatim port of the sandbox's own — `rossjrw/pr-preview-action@v1` publishes each PR's build under `pr-preview/pr-<number>/` on the same `gh-pages` branch and posts/updates a "Storybook preview" section with the live link in the PR description, tearing it down automatically when the PR closes. Added the same `if: ${{ !startsWith(github.head_ref, 'changeset-release') }}` guard `pull-request.yml` already uses (the sandbox version doesn't have this guard, but changeset-release PRs in this repo are pure version bumps with no reason to build a preview).
- **Matched this repo's own existing CI conventions, not the sandbox's verbatim style**: `HUSKY: 0` on the install step (sandbox doesn't set this, but every other workflow in this repo does, to stop Husky's `prepare` hook from trying to install git hooks in CI), and `actions/checkout@v6`/`actions/setup-node@v6`/Node `24` — this repo's already-pinned versions, not the sandbox's (`v6` also happened to already match).
- **Verified `build-storybook` needs no wiring to the `analyze-tokens`/`prebuild` step**: that hook only runs before `npm run build` (the library bundle), not `build-storybook` — confirmed by running `npm run build-storybook` standalone from a clean install, which completed without it.
- **Confirmed output paths are subpath-safe with no config changes**: inspected the built `storybook-static/index.html` — every asset reference is a relative `./...` path, so the same build works unmodified whether served at a domain root (like `recursica-sandbox`'s custom `sandbox.recursica.com` domain) or nested under GitHub's default `https://<org>.github.io/<repo>/` path (this repo has no custom domain configured), and again under the PR-preview action's own further-nested `pr-preview/pr-<n>/` path.
- **Open item, needs repo-admin action, not resolvable via `gh` CLI as used here**: GitHub Pages itself isn't enabled yet for `borderux/recursica-adapter-beam` (`gh api repos/borderux/recursica-adapter-beam/pages` 404s, confirmed before starting). The `gh-pages` branch these workflows push to doesn't exist yet either — `JamesIves/github-pages-deploy-action` creates it automatically on first successful run, but someone with repo admin access still needs to flip on Pages (Settings → Pages → Source → Deploy from a branch → `gh-pages` / `/`) once that branch exists, matching `recursica-sandbox`'s own config (confirmed via `gh api repos/borderux/recursica-sandbox/pages`: `source.branch: "gh-pages"`, `source.path: "/"`).

### 2026-08-28 — Root-caused and fixed `useRecursicaJson must be used within a RecursicaJsonProvider` on `storybook-template`'s bundled demo stories (also closes the "not resolved" `use-sync-external-store` item from the initial-scaffold entry below)

Matt reported the Layout Grids demo story (`Theme/Layout Grids`) throwing `useRecursicaJson must be used within a RecursicaJsonProvider. Please pass recursicaTokensJsonPath, recursicaBrandJsonPath, or recursicaUIKitJsonPath to createPreviewConfig.` — despite `.storybook/preview.tsx` genuinely passing all three JSON paths to `createPreviewConfig`. Confirmed via a debug log that `basePreview.decorators.length === 3`, i.e. the `withRecursicaJson` decorator was present and rendering — the Provider existed, but consumers still couldn't see it. Reproduced on every bundled demo story that calls the hook (`Theme/Layout Grids`, `Theme/Dimensions`, `Tokens/Color`, `Tokens/Opacities`), not just the one Matt hit.

**Root cause**: a Vite `optimizeDeps` dual-module hazard, not a regression in this adapter's own code. `.storybook/preview.tsx` imported `createPreviewConfig` from the `@recursica/storybook-template/preview` subpath export, while the bundled demo stories (which live in `node_modules/@recursica/storybook-template/stories/**` and ship as source, not compiled output) import `useRecursicaJson` from the package **root** (`@recursica/storybook-template`). Both entry points ultimately re-export from the same internal chunk (`dist/preview-BYXgwIGW.js`), which calls `React.createContext(null)` exactly once at module scope — architecturally a single shared singleton. But Vite's dev-server dependency optimizer treats `pkg` and `pkg/subpath` as two independent scan entries; without explicit deduping it can pre-bundle each into its own separate chunk, silently duplicating that `createContext(null)` call. The result: the Provider set up via one copy's `Context.Provider` doesn't satisfy `useContext()` reads against the other copy's `Context` object, even though both trace back to the same source file. This also explains the previously-flagged-and-parked `use-sync-external-store/shim does not provide an export named 'useSyncExternalStore'` bug from the initial-scaffold entry below, which hit the exact same two demo stories (`Theme/Dimensions`, `Tokens/Color`) for the same underlying reason (a different symptom of the same duplicate-chunking hazard, not a separate bug) — confirmed by re-testing those two stories after this fix with zero console errors.

**Fix**: changed the one import in `.storybook/preview.tsx` from `@recursica/storybook-template/preview` to the package root, `@recursica/storybook-template` (which already re-exports `createPreviewConfig` alongside everything else — same export list, no functionality lost). This puts both this adapter's own import and the bundled demo stories' import through the identical optimizeDeps entry, eliminating the duplicate chunk. One-line diff, no Vite config changes needed.

An earlier hypothesis blamed `BeamDarkModeSync.tsx` (added in the light/dark-mode entry below) for shifting Vite's dependency graph and triggering this — plausible on its face since it's the newest addition to the render tree, but not what actually happened: the duplicate-entry-point condition (`/preview` subpath vs. root) predates `BeamDarkModeSync` and was already present when the `use-sync-external-store` symptom was first flagged and parked back in the initial-scaffold entry. `BeamDarkModeSync` likely just changed _which_ chunk-boundary symptom surfaced, not whether the underlying hazard existed.

Verified via Playwright against a freshly-booted (`node_modules/.cache` + `node_modules/.vite*` cleared) Storybook instance: all four previously-failing demo stories now render with zero console errors; a real component story (`Components/🚧 Accordion`) still renders correctly through the full `BeamThemeProvider` → `BeamDarkModeSync` → `Layer` decorator chain; and emitting `storybook-dark-mode`'s `DARK_MODE` channel event still flips `data-bm-theme` from `bm-light` to `bm-dark` as expected — confirming the fix didn't reintroduce the theme-desync bug `BeamDarkModeSync` exists to solve. `check-types` and `lint` both pass clean.

### 2026-08-28 — Test infrastructure re-added, following mantine-adapter's own newly-added pattern (supersedes the "removed the test infrastructure" entry below)

Resolves the "left open, asked rather than guessed" item in the entry directly above. That entry deliberately withheld test-file scaffolding because this repo had previously **removed** test infrastructure on the grounds that mantine-adapter (the genesis adapter) had none of its own — copying it here would have gone beyond what the genesis adapter actually does. That premise no longer holds: Matt confirmed mantine-adapter itself now has unit tests, dom tests, and per-component stub test files, and asked that this adapter follow the same pattern. Adam Adapter relayed the exact shape to replicate.

Added:

- **49 per-component stub test files**, `src/components/<Name>/<Name>.test.tsx`, each containing only `// Placeholder for future <Name> component tests. Add tests here if/when needed.` — one per component _directory_ (a directory holding multiple components, e.g. `ReadOnlyField/`, gets a single stub named after the directory).
- **`vitest.workspace.ts`**: two new projects ahead of the pre-existing `storybook` one — `unit` (node env, `src/**/*.test.ts`) and `dom` (real Playwright/chromium browser, `src/**/*.dom.test.tsx`, deliberately not part of `npm test`, opt-in only via `npm run test:dom`).
- **`package.json`**: `"test": "vitest run --project unit"` and `"test:dom": "vitest run --project dom"`. No existing devDependency was missing (`vitest`, `@vitest/browser`, `@vitest/coverage-v8`, `@playwright/test`, `@storybook/addon-vitest` were already present from the earlier, since-reverted setup).
- **A real unit test**, `src/utils/filterStylingProps.test.ts` — ported from mantine-adapter's own test of the same name, with one adaptation: mantine's example used `mt` (Mantine's margin-prop naming); this repo's `mapLayoutProps` test uses `mTop` instead, matching Beam's own `LAYOUT_PROPS` naming (`m`, `mTop`, `mBottom`, `mBefore`, `mAfter`, `gap`, `rowGap`, `columnGap` — no `mt`/`mb`/`ml`/`mr`). 10/10 pass.
- **A real dom test**, `src/components/Button/Button.styleIsolation.dom.test.tsx` — ported from mantine-adapter's own Button style-isolation test, substituting `MantineProvider`/Mantine's `Button` for `BeamThemeProvider` (`defaultTheme={{ mode: "light" }}`, matching this repo's own `.storybook/preview.tsx` usage) and Beam's real `Button`, and swapping the `data-variant` assertion to this adapter's own marker (`"solid"`). Confirms this adapter's `Button` classes/attributes don't leak onto a plain Beam `Button` mounted alongside it, and that the plain Beam `Button` renders identically (classes, `getComputedStyle`) whether or not the Recursica one is mounted. 2/2 pass. This one was explicitly framed as optional/not-mandatory-per-component by Adam ("add one only where there's a real isolation risk worth guarding") — added here because Button already had a genesis-adapter equivalent to port directly, not because every component needs one.

`check-types`/`lint`/`build`/`npm test`/`npm run test:dom` all pass clean.

**CI question resolved**: flagged for Matt rather than assumed, since this repo previously had a test CI step explicitly _removed_ on the grounds that mantine-adapter had none of its own — that premise no longer held once mantine-adapter added its own test infrastructure. Adam confirmed the monorepo's root `pull-request.yml` already runs `npm run test` for every workspace via turbo, so mantine-adapter's unit tests are enforced there; beam-adapter, as a standalone repo, needed its own equivalent step added directly. Matt's answer: enforce **unit tests only** in CI; dom tests must never run as part of build or CI (matches mantine-adapter, which also doesn't run dom tests in CI). Added a `Test` step (`npm run test`) to `.github/workflows/pull-request.yml`, after `Build`. **Not** added to `precommit` — mantine-adapter itself has no `precommit` script at all, so there's no parity case for adding test there, and Matt's instruction only named CI.

### 2026-08-28 — Stub `IMPLEMENTATION_NOTES.md` seeded from the integration report; retrofitted all 35 remaining stubs

Matt clarified the intended sequence: write the integration report (step 2), then stub every component _informed by it_ — story showing WIP, empty CSS module — and write an `IMPLEMENTATION_NOTES.md` at stub time that carries the report's findings forward, so a later implementer works from notes instead of re-reading the whole report to find their one component's row.

Updated step 9 to require this as a new item 5 (component-specific `IMPLEMENTATION_NOTES.md`, seeded from `docs/ADAPTER_INTEGRATION_REPORT.md` §9, explicitly flagged as a pre-implementation survey rather than a substitute for step 10's own prop audit), renumbering the following two items. Also fixed a stale naming inconsistency caught while editing step 10 — item 4 said `{COMPONENT}_IMPLEMENTATION_NOTES.md`, but every real component's folder (`Button/IMPLEMENTATION_NOTES.md`, etc.) uses the plain, unprefixed name; updated the doc to match actual practice rather than leave a description that's never matched reality.

**Retrofitted all 35 remaining stub components** (everything not yet implemented, per `docs/ADAPTER_INTEGRATION_REPORT.md` §9's build-order list) with a seeded `IMPLEMENTATION_NOTES.md`, each seeded with that component's category (Easy/Requires Work/Does Not Exist), Beam candidate(s), and the specific gap/finding already documented in the report — plus, where relevant, a cross-reference to a related component's own gap (e.g. `Checkbox`/`Radio`/`Switch` all point at each other for the shared "no description slot on the bare control" pattern; `NumberInput`/`TransferList` note they depend on `TextField`/`Checkbox` respectively; `Card`'s notes flag that building it should resolve two known `Grid` `adapter-tester` failures as a side effect). `check-types`/`lint`/`build` all pass clean — pure documentation, no source touched.

**Left open, asked rather than guessed**: Matt's message also mentioned "the test file as empty" as part of the per-component stub scaffold, alongside the story and CSS module. Given this repo's own history explicitly removed test infrastructure earlier (`npm run test`, `vitest.workspace.ts` wiring — see the "removed the test infrastructure" entry further down, an explicit instruction not to re-add without being asked), this wasn't assumed either way — asked Matt directly what "test file" means here before adding anything, rather than guess and either reintroduce something explicitly rejected before or silently skip something he actually wants.

### 2026-08-28 — Light/dark mode: found and fixed a real Beam/Recursica theme desync, added report question 10

Matt asked one more question after the integration report shipped: how does the underlying kit handle light/dark mode, how do you switch, and how does that interact with `RecursicaThemeProvider` — "we need to ensure light vs dark mode switch happens for both." Treated as an action item, not just a question to write up.

**Real bug found and fixed, verified live, not just reasoned about from the types:**

- `BeamThemeProvider`'s `defaultTheme` prop is initial-value-only — confirmed by reading Beam's actual compiled implementation (a `next-themes`-style pattern: seeds internal state once on mount, never re-reads the prop). The only way to switch Beam's theme after mount is its own exported `useBeamTheme()` hook (`setMode`/`toggleMode`/`setAccent`/`setProductType`, plus `systemMode` state). `RecursicaThemeProvider`'s `theme` prop, by contrast, genuinely is reactive (a `useEffect` keyed on `[theme]`) — but nothing connects the two by default.
- This adapter's own `.storybook/preview.tsx` had exactly this bug: Storybook's dark-mode toolbar toggle is wired automatically via `@recursica/storybook-template`'s shared decorator, which only knows about Recursica's side. Verified via Playwright: clicking it flipped `data-recursica-theme` (`light` → `dark`) correctly, while `data-bm-theme` stayed completely unchanged.
- **Fixed**: added `BeamDarkModeSync` (`.storybook/BeamDarkModeSync.tsx`) — a small component inside `BeamThemeProvider` that listens for the same `storybook-dark-mode` addon channel event the shared decorator already listens to, and calls `useBeamTheme().setMode()` in response. Re-verified via Playwright, both directions: `data-bm-theme`/`data-recursica-theme` now flip together (confirmed `bm-light`/`light` → `bm-dark`/`dark` → back), plus a screenshot showing the Button correctly re-themed. In its own file rather than declared inline in `preview.tsx` — `react-refresh/only-export-components` correctly flagged the inline version, moved rather than ignored.
- **Documented the general-purpose version for real consumer apps** in `SETUP.md` §4, which previously only said "keep both providers' initial values in sync" — true but incomplete, since it doesn't cover runtime switching at all. Added the actual pattern: a `SyncedRecursicaTheme` component between the two providers that reads `useBeamTheme().theme.mode` and feeds `RecursicaThemeProvider`'s `theme` prop from it, making Beam's own theme context the single source of truth so any `toggleMode()` call anywhere in the tree keeps both in sync automatically — the same fix shape verified working in Storybook, generalized.
- Added as **step 2's item 9** (renumbering the former item 9, component mapping, to item 10) in `docs/CREATING_AN_ADAPTER.md`, and as **`docs/ADAPTER_INTEGRATION_REPORT.md`'s §10** — explicitly instructing future adapters to verify a live toggle with a real before/after check, not just confirm each side independently supports light/dark.

`check-types`/`lint`/`build` all pass clean (the `react-refresh` warning from the inline version is gone after extracting the component to its own file).

### 2026-08-28 — `docs/ADAPTER_INTEGRATION_REPORT.md` written; added as new step 2, every subsequent step renumbered

Explicit instruction: before scaffolding starts, an adapter needs a written reconnaissance report answering 9 specific questions about the underlying UI kit's styling system, layout/typography/form-control primitives, and a full component-by-component build-order plan — not something to work out piecemeal as components get built. Added as **step 2** (`docs/CREATING_AN_ADAPTER.md`), immediately after "understand the genesis adapter" and before "decide what's monorepo-specific" — renumbering every subsequent step (old 2–10 → new 3–11) and fixing all ~35 internal `step N` cross-references throughout this file, including inside older decisions-log entries, per this doc's own established precedent (see the "new step 8" entry below, from when step 7/styling-system was inserted the same way) of treating cross-references as always describing current numbering, not frozen historical state.

Since this repo already has 16 real components by the time this was written, the report itself was written retroactively — same approach `docs/STYLING_SYSTEM.md` used. Rather than re-deriving answers from scratch, it draws on: existing docs (`STYLING_SYSTEM.md` for styling-mechanism questions 1/3/4), a live review of `SETUP.md`/`USAGE.md` against what's actually verified working in `.storybook/preview.tsx` (question 2 — found both docs accurate, but traced and documented one apparent-but-not-real discrepancy around `RecursicaThemeProvider` not being used in the Storybook preview, and fixed one real staleness in `USAGE.md`'s "no components yet" status line), a dedicated research agent's evidence-based sweep of every Beam `.d.ts`/compiled-source file for layout/typography/form-control primitives and a full 49-component mapping table (questions 5–9), and this file's own decisions log for what's already been verified against `adapter-tester` for the components built since the agent's research ran (Flex/Stack/Group/Grid/Text/Title/FormControlWrapper's batch — folded in so the report doesn't understate what's already confirmed working).

**Real, notable finding surfaced synthesizing all of this together**: the report's own component-by-component build order (`docs/ADAPTER_INTEGRATION_REPORT.md` §9) is a plan that this adapter has, in practice, already been following loosely without it being written down yet — the components built so far (Button, Loader, layout primitives, typography primitives, the form-control family) all fall into what the report's evidence would categorize as EASY or a necessary DOES-NOT-EXIST build (`FormControlWrapper`), not REQUIRES-WORK items like Checkbox/Radio/Switch or DOES-NOT-EXIST items like DatePicker/Timeline. Confirms the build order this report recommends going forward (Link, Toast, Tooltip, Pagination, Popover, ... down to Table/Container/NumberInput/TimePicker/Timeline/TransferList/Tree/DatePicker as the hardest) is a real, evidence-backed continuation of what's already been working, not a break from it.

`check-types`/`lint`/`build` all pass clean after the renumbering (pure documentation change, no source touched). Left on the shared working tree's current branch (`form-control-and-layout-components`) alongside unrelated in-progress `TextField`/`ReadOnlyField`/`FormControlWrapper` work already sitting there uncommitted — didn't touch or interfere with those files, confirmed by diffing only the files this pass actually changed (`USAGE.md`, `docs/CREATING_AN_ADAPTER.md`, new `docs/ADAPTER_INTEGRATION_REPORT.md`).

### 2026-08-28 — TextField, ReadOnlyField, WithReadOnlyWrapper, ReadOnlyTextField: thirteenth through sixteenth real components; a real `FormControlWrapper` bug found and fixed along the way; simplified `AssistiveElement`

Per Adam's reference trace of mantine-adapter's real `TextField` → `WithReadOnlyWrapper` → `ReadOnlyField` → `ReadOnlyTextField`/`EmptyValueRenderer` chain (no Mantine-specific logic in the middle two files — ported near-verbatim), plus a fresh read of Beam's actual compiled `TextField.css`/`TextField.es.js` (not guessed) to confirm Beam has no naked-input primitive to reuse.

**`TextField`**: Beam's real `TextField` is a monolith (its own `LabelProvider`/`HelperTextProvider` rendering `Label`/`HelperText` internally) — reusing it would double-render against this adapter's own `FormControlWrapper`, same problem mantine-adapter solves by using Mantine's low-level `<Input>` instead of `<TextInput>`. Since Beam ships no equivalent low-level primitive at all, built a private `NakedTextField` (an internal, unexported component) replicating Beam's exact box model read from its real CSS — border/background/radius/padding on a wrapper div, not the `<input>` itself; focus ring via `:focus-within` (not `:focus`, since the input's own outline is suppressed); disabled/read-only/error as data-attribute-keyed variants — but driven entirely by Recursica's own `--recursica_ui-kit_components_text-field_*` tokens, not Beam's literal values. Focus ring uses the shared brand-level focus tokens (`--recursica_brand_states_focus_*`) via a `recursica-allow-brand` exemption directive, matching mantine-adapter's own documented, sanctioned exception for this one case.

**Real bug found and fixed in `FormControlWrapper` while wiring this up**: neither this adapter's own `FormControlWrapper` (before today) nor mantine-adapter's actually wire the native `<label for="...">` → field `id` association — both only pass `id` (never `htmlFor`) to `Label`. Confirmed mantine-adapter has the identical gap by reading `@mantine/core`'s real `InputLabel` source (`id`/`htmlFor` are fully independent props there too). First fix attempt (passing `id` to _both_ the `<Label>` and the cloned child) turned out to be a real regression, caught via live testing, not assumed correct: giving the label element the _same_ `id` as the field creates a duplicate-id collision — `label.control` came back `null` in a real browser, and clicking the label did nothing, because the browser's own `for`/`id` resolution hit the label itself first (same id, earlier in the DOM). Fixed properly: `Label` now gets only `htmlFor={id}` (no `id`), and the child still gets `id` cloned onto it. Verified via Playwright: `label.control === input` is `true`, and `page.click('label')` now genuinely focuses the field. Worth porting back to mantine-adapter, which still has the original (never-worked) version of this.

**`WithReadOnlyWrapper`/`ReadOnlyField`/`ReadOnlyTextField`**: no Beam-specific logic in any of the three (matches mantine-adapter's own files, which are UI-kit-agnostic too) — ported the three-way branch (custom `readOnlyComponent` / generic `ReadOnlyField` fallback / live `activeComponent`), the `type`-dispatch-always-renders-through-`ReadOnlyTextField` behavior, and the `EmptyValueRenderer`/`.check` static-method convention exactly. Not ported: `ReadOnlySwitchField`/`ReadOnlyBooleanField` — confirmed via the mantine research pass that neither is actually wired into `ReadOnlyField`'s own dispatch there either; nothing in this adapter needs them yet.

**`AssistiveElement` simplified** (separate from the above, prompted by Matt's and Adam's review of the previous pass): dropped the wrapper `<div ref={ref}>`/`forwardRef` entirely — Beam's `HelperText` already renders its own root `<div>`, and nothing in this adapter ever consumed a ref to `AssistiveElement`. Re-verified the icon/text vertical-centering fix from the prior pass still holds with one less DOM layer. Real, accepted tradeoff: the public export's `wrapComponent()` wrapper always attaches a `forwardRef`, so an external consumer who explicitly attaches a `ref` to the published `AssistiveElement` will see React's standard "function components cannot be given refs" dev warning (the ref is simply dropped, not a crash).

**Verified for real, not just "it compiles":** booted Storybook + Playwright. Confirmed `label.control`/click-focuses-input (see above), `aria-describedby`/`aria-errormessage` land on the real `<input>`, disabled/read-only/error visual states render the correct token-driven colors, `leftSection`/`rightSection` icons render and recolor per state, side-by-side layout composes correctly, `ReadOnlyField`'s empty-value fallback renders "N/A". Screenshotted every `TextField` story. `check-types`/`lint`/`build` all pass clean (the token-analyzer's brand-layer-violation check required adding the `recursica-allow-brand` exemption comment to `TextField.module.css` for the focus tokens — same requirement mantine-adapter's own file documents). Story names/content matched to mantine-adapter's own `TextField.stories.tsx` exactly (learned from the previous pass's review that content mismatches inflate `adapter-tester` diffs for reasons unrelated to the actual components) — not yet run against `adapter-tester` itself.

### 2026-08-28 — Review pass on Label/AssistiveElement/FormControlLayout/FormControlWrapper: one real component bug, one real story-fixture bug, both fixed and re-verified

Matt reviewed the previous pass and reported two problems: `AssistiveElement`'s text wasn't vertically centered with its icon, and `FormControlLayout` "does not match mantine at all." Root-caused both for real rather than guessing.

**`AssistiveElement` — real bug, fixed.** Beam's own `.bm-helper-text` sets `display:flex` but never sets `align-items` (confirmed in its real compiled CSS) — the icon and text default to the flexbox initial alignment, which visually rides the icon above the text's vertical center. A zoomed screenshot comparison against mantine-adapter's own (correctly centered) rendering confirmed it. Fixed with `align-items: center` on `.beamHelperText`. Re-verified: all three `AssistiveElement` stories now pass `adapter-tester` outright.

**`FormControlLayout` — the component was actually fine; the demo was broken.** Computed-style comparison against mantine-adapter's real Storybook showed `FormControlLayout`'s own CSS is byte-identical to the genesis adapter's (`max-width: var(--form-control-max-width, 100%)`), and 2 of 3 of its own stories already passed `adapter-tester` before touching anything. The actual problem: no story in this adapter ever passed a real `controlMaxWidth`, so every composed field rendered at the `100%` fallback width — a giant, oversized-looking input instead of a normal form field. Confirmed the fix precisely: mantine-adapter's real `TextField` passes `controlMaxWidth="var(--text-field-control-max-width)"`, resolving to `464px`; this adapter's own `--recursica_ui-kit_globals_form_field_size_max-width` token resolves to the identical `464px`. Added that as a `FormControlWrapper` story-level default. Also found and fixed a genuinely separate bug in the stub `NakedInput`: `font: "inherit"` silently fails on `<input>` elements (browsers don't inherit body font-family into form controls by default), so it was rendering in the browser's serif fallback instead of Recursica's typography — fixed by setting the font explicitly from the same `--recursica_ui-kit_components_text-field_properties_text_*` tokens a real `TextField` will use. Also aligned story copy/content to match mantine-adapter's own stories exactly (including rebuilding `NativeChildrenDirectly` around a checkbox, not a text input) — mismatched content was inflating `adapter-tester`'s pixel diffs for reasons unrelated to the actual components.

**Result:** composed field width now matches mantine-adapter's exactly (464px, confirmed via `getBoundingClientRect`), `Required Architecture`/`Native Children Directly` now pass `adapter-tester` outright, and `Default`/`Visual Error State`/`Without Assistive Icons` dropped from "looks nothing alike" to a moderate, well-understood diff (~3800–4900px) attributable entirely to the stub `NakedInput`'s own native styling versus mantine's real, more polished `TextField` — the same accepted category as `Grid`'s `Swatch` standing in for a not-yet-real `Card`. Full traces in each component's own `IMPLEMENTATION_NOTES.md`. `check-types`/`lint`/`build` all pass clean.

### 2026-08-28 — Label, AssistiveElement, FormControlLayout, FormControlWrapper: ninth through twelfth real components

Planned first (`PLANS/BEAM_FORM_CONTROL_PLAN.md` in the workspace, not this repo — a plan doc, not shipped code), reviewed by Matt, then built in order: `Label` → `AssistiveElement` → `FormControlLayout` → `FormControlWrapper`. Unlike `Flex`/`Stack`/`Group`/`Grid`, these four **keep** the formal `Recursica<Name>Props` contract from `@recursica/adapter-common` (confirmed by Adam — different rule than the layout components).

**Key finding shaping the whole batch:** the genesis adapter's (mantine-adapter) architecture here is "bypass the kit's own field-composition system and hand-build everything" — it uses Mantine's bare `Input.Label` standalone (not `Input.Wrapper`/`InputContext`), builds `AssistiveElement` from scratch (Mantine has none), and hand-wires ARIA via `React.cloneElement` in `FormControlWrapper`. Beam's own architecture is the opposite: `TextField`/`TextArea` take pre-built `<Label>`/`<HelperText>` elements and wire them together _internally_ via two React Contexts (`LabelProvider`/`HelperTextProvider`, from `Form/contexts/`) plus a `useAccessibleId` hook — there's no exported "form field shell" to delegate to, and that Provider pattern only helps a child Beam's own `TextField` controls internally, not an arbitrary composed child. Same bypass-and-hand-build approach applied here instead of forcing Beam's Provider pattern onto something it wasn't built for.

- **`Label`** wraps Beam's real `Label` component, used completely standalone (no `LabelProvider` ancestor) — confirmed via its compiled source (`lib/Label/Label.es.js`) that all context-fallback props (`htmlFor`/`required`/`disabled`/`id`) are only _fallbacks_, explicit props always win. Beam's `optional` maps directly to `labelOptionalText` (real 1:1 reuse). `labelWithEditIcon`/`labelActionArea`/`labelSize`/`labelAlignment` have no Beam equivalent — added as one extra sibling (not a full flex/order rebuild like mantine-adapter needs, since Beam's `Label` already handles text/required/optional internally). Beam renders its required-asterisk as a CSS `:after` pseudo-element, not a stylable DOM node (unlike Mantine's named `classNames.required` styles-API slot) — recolored by targeting the identical selector at equal specificity, later in the cascade, no `!important`. `ref` attaches to this component's own wrapping `<div>`, not `HTMLLabelElement` like the genesis adapter — Beam's `Label` is a plain function component, no `forwardRef` at all.
- **`AssistiveElement`** wraps Beam's real `HelperText` — actually a _better_ position than the genesis adapter had (Mantine has no equivalent at all). `assistiveVariant` "help"/"error" → Beam's `appearance` "info"/"negative"; `assistiveWithIcon` → Beam's `hideIcon`, inverted. **Real, documented divergence found and kept, not silently papered over:** read Beam's actual compiled source (`chunks/form.context.BDYA2JQs.js`) and confirmed `HelperText` hardcodes `role="status"`/`aria-live="polite"` for every appearance — its JSX spreads the caller's rest props _before_ literally setting those two, so any `role` a caller passes is discarded, there is no way to get Beam's `HelperText` to render `role="alert"`. Chose to keep Beam's own native accessible behavior rather than fight it with a redundant wrapper (which would create two nested ARIA live regions around the same text) — the genesis adapter's `role="alert"`-for-error default is not replicated here.
- **`FormControlLayout`** — no Beam equivalent (confirmed, nothing in `beam-react`/`beam-shared`). Near-direct port of the genesis adapter's own implementation, which also doesn't wrap a Mantine component here — same `data-form-layout`/CSS-custom-property structure, same shared `--recursica_ui-kit_globals_form_*`/`--recursica_ui-kit_components_label_*` token names (confirmed present in this adapter's own `recursica_variables_scoped.css` before porting, not assumed).
- **`FormControlWrapper`** — the orchestrator, same architecture as the genesis adapter's: `useId()` for ids, `React.cloneElement` for ARIA wiring onto the child, error/help mutual exclusion (error always wins), `Label` construction, delegates the actual box layout to `FormControlLayout`. No `withAsterisk` alias (that only exists on the genesis adapter's version via Mantine's own `InputWrapperProps` it types against — nothing analogous here, just a plain `required?: boolean`). `helperText` is declared in the shared contract but not read — matches the genesis adapter's own implementation exactly, not a gap introduced here.

**Verified for real, not just "it compiles":** booted Storybook + Playwright at each of the four steps. `Label`: asterisk color/positioning, optional-text suppression under `required`, edit-icon replacing the asterisk. `AssistiveElement`: icon presence/absence, correct Beam `appearance` class + icon shape per variant, `role="status"`/`aria-live="polite"` confirmed present in the real DOM. `FormControlLayout`: stacked vs. side-by-side column behavior, left-column-always-present-even-when-empty in side-by-side. `FormControlWrapper`: composed against a stub `<input>` "naked field" (this adapter has no real `TextField` yet) — confirmed in the real DOM that `aria-describedby`/`aria-errormessage` land on the actual input element (not just present in the React tree), error text correctly replaces help text, side-by-side layout renders the label column + red asterisk + input + helper text as one coherent two-column row. Screenshots taken at each step. `check-types`/`lint`/`build` all pass clean.

Not yet compared against mantine-adapter via `adapter-tester` — no shared story shape exists yet since this adapter has no real field component to render `Label`/`AssistiveElement`/`FormControlWrapper` through the same way mantine-adapter's own stories do (they route through a real `TextField`). Flagged as a follow-up once a real field component (`TextField`) is built — separate future work, not part of this batch per Matt's own scoping.

### 2026-08-27 — Text and Title: third and fourth real components

Both wrap the same underlying `@viasat/beam-react` `Text` component — Beam has no separate `Title`/`Heading` primitive; one component covers body, detail, label, and heading roles via its `kind` prop. `Text`'s `variant` and `Title`'s `order` each map to a `kind` value purely to give Beam a sane internal baseline; the actual visual result comes entirely from Recursica's own `recursica_brand_typography_<variant>`/`recursica_brand_typography_h<order>` class (same mechanism as the genesis adapter, mantine-adapter, which has no `Text.module.css`/`Title.module.css` at all for the same reason — kept both as empty files here per step 9's "every component gets the same CSS Modules hook" convention). `component` maps to Beam's own polymorphic `as` prop. `kind`/`as`/`theme` are the only Beam props removed (`theme` for the same "global concern" reasoning as Button/Loader); Beam's other native `Text` props (`color`, `bold`, `alignment`, `wordBreak`, `truncate`, `block`, `compact`, `strikethrough`, `underline`, `italic`) pass through unblocked, matching mantine-adapter's own permissive precedent for `Text`. Full reasoning in `Text/IMPLEMENTATION_NOTES.md` and `Title/IMPLEMENTATION_NOTES.md`.

Story titles renamed from `Components/🚧 Text`/`Components/🚧 Title` to `UI-Kit/Text`/`UI-Kit/Title` per step 9, item 6, and story names (`Default`/`StaticVariations`) matched to mantine-adapter's own so `adapter-tester` can compare them directly — not yet run against the harness as part of this pass, flagged as a follow-up.

### 2026-08-27 — Text/Title run against `adapter-tester`: 2 of 4 stories fail, root-caused to a mantine-adapter story bug, not this adapter

Ran `npm run adapter-tester:automated` (full 277-story suite, twice, to rule out flakiness) against the real thing this time. `ui-kit-text--default` **passes** both runs. `ui-kit-title--default` (13600 diff px), `ui-kit-text--static-variations` (7677), and `ui-kit-title--static-variations` (8824) all **fail** consistently across both runs — real, reproducible diffs, not the navigation-timeout flakiness seen in an earlier run (worker contention while both dev servers were cold; the second run, with warm servers, replaced the timeouts with real numbers).

**Root-caused, not left as "probably fine":** booted this adapter's own Storybook (`-p 6098`) and the local `mantine-adapter` checkout's Storybook (`-p 6097`) side by side and compared real computed styles/box metrics via Playwright, not just reasoning about the code.

- The actual typography is identical on both sides — `font-family: Lexend`, `font-size: 40px`, `font-weight: 700`, `line-height: 42px`, `margin: 0`, `padding: 0` for `h1`, confirmed via `getComputedStyle`. No token or CSS mapping bug.
- The difference is layout width, traced to a **stale double-`<Layer>` wrap on mantine-adapter's side**. `mantine-adapter`'s `.storybook/preview.tsx` has the same global decorator this adapter does (`withLayer: true, layer: 0`, wraps every story in `<Layer style={{padding:"48px"}}>` by default, from the shared `@recursica/storybook-template` preview config) — but `Text.stories.tsx`/`Title.stories.tsx` in mantine-adapter _also_ manually wrap their `Default`/`StaticVariations` `render()` in their own `<Layer layer={0} style={{padding:"48px"}}>`, stacking on top of the automatic one. Confirmed via measured DOM rects: mantine's `h1` sits at `x:112,y:112` (96px inset — two 48px paddings) inside a `676px`-wide content area, while this adapter's (and mantine's own `Button - Default`, which does _not_ manually wrap) sits at `x:64,y:64` (48px — one padding) inside `772px`. Mantine's `Button.stories.tsx`/`Loader.stories.tsx` already had this manual wrap removed when the auto-decorator was introduced (see the two entries above this one in the log) — `Text.stories.tsx`/`Title.stories.tsx` were never updated the same way.
- **Consequence:** at the narrower 676px width, mantine's 40px `H1` "Semantic H1 Document Boundary" wraps to 2 lines (`h1` box height 84px) where this adapter's identical text at 772px stays on 1 line (42px) — a large, real pixel diff caused entirely by the extra padding, not by any difference in the actual `Title`/`Text` implementation. `StaticVariations`' 6-line block sees the same narrower-width effect across both stories, which is why both `Text` and `Title`'s `StaticVariations` also fail while their `Default`s pass or fail depending on how sensitive that particular string is to the width difference (`Text - Default`'s short body sentence doesn't happen to wrap differently at either width; `Title - Default`'s large H1 does).
- **Not fixed here** — this is a bug in `mantine-adapter`'s own story files (a different repo/package), the same category as the earlier-documented `Button - Truncated Label` finding ("real bug in Mantine's own reference implementation, not beam-adapter"). Flagged for whoever owns `mantine-adapter`: drop the manual `<Layer>` wrap from `Text.stories.tsx`/`Title.stories.tsx`'s `Default`/`StaticVariations` renders, matching what `Button.stories.tsx`/`Loader.stories.tsx` already do.

This adapter's `Text`/`Title` implementation itself is unaffected — the one story that isn't sensitive to the width difference (`Text - Default`) already passes outright, and the failures fully explain themselves via the padding math above with no residual unexplained diff.

### 2026-08-27 — Flex, Stack, Group, Grid: fifth through eighth real components; new layout-components rule; found and fixed a real style-clearing bug

**New rule (Matt):** unlike this adapter's other components, `Flex`/`Stack`/`Group`/`Grid` get **no** formal `Recursica<Name>Props` contract in `@recursica/adapter-common` — mantine-adapter/mui-adapter's existing ones (`RecursicaFlexProps` etc., an abstraction duplicating each underlying kit's own layout prop shape) were identified as a mistake. Instead: (1) the four components exist, matching Mantine's own definitions; (2) `rec-*` Recursica size tokens are supported on spacing props where reasonably easy, not a hard requirement; (3) each documents its purpose/use per Mantine's own docs; (4) since Beam has no native equivalent for any of the four, each is built here mirroring `@mantine/core`'s own prop shape directly. Full reasoning in `Flex/IMPLEMENTATION_NOTES.md` (shared across all four) plus each component's own notes file. `WithRecursicaSpacing<T>` (still imported from `@recursica/adapter-common`) is _not_ part of the mistake — it's a generic type-only augmentation for `rec-*` token typing, not a per-component contract, so it stays.

None of the four use the `RecursicaOverStyled` gatekeeper — same precedent mantine-adapter's own `Flex`/`Stack`/`Group`/`Grid` already set (a layout primitive has no internal "look" to protect; callers need `className`/`style`/native layout props to pass through freely).

### 2026-08-27 — Full `adapter-tester` re-run against `mantine-adapter@0.50.4`/`adapter-tester@2.0.2`: confirms the story fix landed; every remaining failure traced to a known non-bug, not this adapter's Text/Title/Flex/Stack/Group/Grid

Matt asked to pull the latest and confirm — bumped `@recursica/adapter-tester` to `2.0.2` (`^2.0.1` already allowed it, `npm install` picked it up).

**Real gotcha found first:** the harness caches its own throwaway `@recursica/mantine-adapter` install under `.adapter-tester/mantine-harness/node_modules` and does **not** refresh it on every run. A same-day re-run still tested against the _old_ `0.50.2` (pre-fix) even after `mantine-adapter@0.50.4` (with the Text/Title story fix) was published — confirmed by reading the cached copy's `Title.stories.tsx` directly, still showing the old manual `<Layer>` wrap. Deleting `.adapter-tester/` and re-running forced a fresh install of `0.50.4`. Worth knowing for next time: **delete `.adapter-tester/` before trusting a re-run is testing latest**, don't just bump the devDependency version.

**With the fresh harness install, real progress:** `Button - Truncated Label` and `Title - Default`/`Title - Static Variations` all now **pass** (previously failing) — confirms both the button-overflow fix and the Text/Title story fix (dropping the double `<Layer>` wrap) landed and work. `Text - Static Variations` dropped from 7677 to 3596 diff pixels — just barely over the 3500 threshold, and the two screenshots are visually indistinguishable side by side; almost certainly anti-aliasing/font-rendering noise between two independent page loads, not a real difference (same category as `Loader - Layer Two Oval`'s animation-timing flakiness).

**16 failures remain; root-caused every one, not left as "probably fine":**

- `Flex - Static Gap Small Column`/`Large Row`, `Group - Static Gap Small`/`Large`, `Stack - Static Gap Small`/`Large` (6 failures): confirmed via computed styles that the container CSS is byte-identical between beam-adapter and mantine-adapter (same `gap`, `flexDirection`, `alignItems`, `justifyContent`, same container rect). The diff is entirely inside the 3 `<Button>` children each story renders — Beam's `Button` renders visibly narrower/shorter than Mantine's for the same label (same known cross-adapter sizing difference `Button - Truncated Label` surfaced originally). These "static gap" stories use 3 solid buttons with no other content; `Default` (2 buttons + a `<Text>`) has less cumulative colored surface area and stays under the fixed 3500px threshold, while the more button-heavy variants tip over it. **Not a Flex/Stack/Group bug** — their own layout math is verified pixel-identical; this is Button's already-known appearance difference, amplified by story content, not something to fix here.
- `Grid - Grow`, `Grid - Custom Column Count` (2 failures): confirmed via screenshots that column positions/widths are pixel-identical between both sides. The diff is the placeholder `Swatch` div (a plain bordered box, since this adapter's `Card` isn't real yet — see `Grid.stories.tsx`'s own comment) rendering with different corner-radius/padding than Mantine's real `Card` component used in the same stories. **Not a Grid bug** — a known, already-documented content-fixture gap that resolves once `Card` is implemented for real, not before.
- `Layer - Contents Only`/`Default`/`Nested Layers`, `Loader - Layer Two Oval` (4 failures): same pre-existing, already-documented issues from earlier passes (global body font-family gap in `.storybook/preview.tsx`; Loader's spinning-animation timing) — unrelated to this pass's work, not re-investigated here.

**Net result: zero real bugs found in Text/Title/Flex/Stack/Group/Grid this pass.** Every failure is either a known pre-existing gap, a marginal noise-level diff, or an expected content/fixture difference (Button's own cross-adapter look, a stand-in Swatch for a not-yet-built Card) — not a defect in this pass's actual layout math or typography mapping. `check-types`/`lint`/`build` still pass clean after the `adapter-tester` bump.

**`Grid`/`Grid.Col` mechanism**: replicated Mantine's actual bootstrap-style flex grid (negative-margin container + per-column padding + percentage `flex-basis`/`max-width` from `span`/`columns`) by reading its compiled CSS/JS directly (`@mantine/core`'s `styles/Grid.css`, `GridCol/GridColVariables.mjs`), not guessing the formula. `gap` (not Mantine's `gutter`) for consistency with `Flex`/`Stack`/`Group`, matching mantine-adapter's own renaming choice. Per-breakpoint responsive `span`/`order`/`offset` objects and `visibleFrom`/`hiddenFrom` are not implemented — a considered scope cut, documented in `Grid/IMPLEMENTATION_NOTES.md`.

**Real bug found and fixed, not just "made it compile":** `gap` silently had no effect on `Flex`/`Stack`/`Group` — verified via a live Storybook + Playwright check, not just reading the code back. Root cause: each component's `style` object included `rowGap`/`columnGap` (or `align`/`justify`) keys whose _value_ was `undefined` when the caller didn't pass them. React applies an explicit `undefined` style value by clearing that DOM property to `""` rather than skipping the key — and clearing `rowGap`/`columnGap` to `""` _after_ `gap` had already set them wipes out the entire `gap` shorthand (confirmed directly against a real `element.style`: setting `gap` then `rowGap = ""; columnGap = ""` empties `cssText` completely). Fixed with a new `omitUndefinedStyles` helper in `src/utils/filterStylingProps.ts` that drops undefined-valued keys before the style object reaches React; applied in all four components. Re-verified after the fix: `rec-default`/`rec-sm`/`rec-xl` all resolve to their correct pixel values (8px/4px/24px) via `getComputedStyle`.

Verified for real: booted Storybook, screenshotted `Flex`/`Stack`/`Group`'s `Default` stories (Button/Button/Text laid out correctly with gap) and `Grid`'s `Default`/`Offset`/`Grow` stories (3-column span-4 layout, offset column pushed right, `grow` filling the row edge-to-edge). `check-types`/`lint`/`build` all pass clean. Story names matched to mantine-adapter's own (`Default`/`StaticGapSmallColumn`/`StaticGapLargeRow` for `Flex`, `Default`/`Offset`/`Grow`/`CustomColumnCount` for `Grid`, etc.) where a Beam-side equivalent makes sense — `Grid`'s `ResponsiveSpans`/`VisibleHiddenFrom` stories are skipped since those features aren't implemented here. Not yet run against `adapter-tester` — flagged as a follow-up, same as Text/Title.

**Verified for real**, not just that it compiles: booted Storybook and drove it with Playwright. `Text` renders a real `<p>` (`ui-kit-text--default`) with class `recursica_brand_typography_body` and computed `font-family: "Bellota Text"`/`font-size: 16px` — distinct from Beam's own `bm-sem-typo-body-md` defaults, confirming the override actually wins the cascade, not just that the class is present. `Title` renders a real `<h1>` (`ui-kit-title--default`) with class `recursica_brand_typography_h1`, computed `font-family: Lexend`/`font-weight: 700`/`font-size: 40px`. Screenshotted both `StaticVariations` stories — all 6 `Text` variants and all 6 `Title` orders render visibly distinct from each other. `check-types`/`lint`/`build` all pass clean.

Uncommitted on branch `text-title-components` (worktree at `recursica-worktrees/text-title-components`), ready for review.

### 2026-08-27 — `@recursica/adapter-tester@2.0.1` published; both fixes confirmed end-to-end with the real package

The Layer-decorator fix and the skip-logic fix (both entries below) shipped as `2.0.1` (PR #537 merged to main, changeset added retroactively since the merge itself didn't include one, then published). Upgraded here (`^2.0.0` already satisfied it — no `package.json` range change needed, just `npm install`) and re-ran the full, unfiltered suite against the genuinely published package — not a hand-patched local copy this time.

**Result: 277 stories in 52 seconds. 263 skipped, 9 passed, 5 failed** — up from 4 passed/10 failed against the un-fixed 2.0.0, confirming both fixes are real and effective together: every Button story that failed purely due to the missing Layer wrapper (Default, Disabled Solid, Layer One Solid, Loading, Solid Default) now passes.

**The 5 remaining failures**: `Button - Truncated Label` and `Loader - Layer Two Oval` are the same two already root-caused in the entry below (Mantine's own Button doesn't respect a constraining width; part animation-timing flakiness, part a real Loader-centering difference). Three are new, surfaced only now that a full run finishes in practical time: `Layer - Default`, `Layer - Contents Only`, `Layer - Nested Layers`. Took one screenshot comparison (Layer - Default) to get a first read: same content, same Layer surface, but the body text visibly renders in a different font between the two (Mantine's sans-serif vs. what looks like the browser's serif fallback on beam-adapter's side) — most likely beam-adapter's `.storybook/preview.tsx` never sets a default body `font-family` the way Mantine's stack does (Beam's own font package likely only styles Beam-specific component classes, not generic body text). Not chased to a fix — this is a global Storybook preview setup gap, not a Layer/Button/Loader component bug, and out of scope for "confirm the upgrade works." Flagged as a real follow-up.

Raised by Matt after the entry below: a fresh/partially-built adapter (this repo has 2 real components against Mantine's ~49) makes every full `adapter-tester:automated` run extremely slow, and floated removing all stub component stories as the fix.

**Investigated before acting — removing stub stories would not have helped.** Read `runVisualRegression`'s actual algorithm: it generates one test per story in the _source of truth's_ index, then unconditionally navigates the target's iframe url for that same id and waits out the full per-test timeout for `#storybook-root` to appear. It never checks whether the target's own Storybook has that story at all. Whether beam-adapter has 47 stub stories with non-matching ids or zero stories for those components, the outcome for each of Mantine's ~49 real-component stories is identical: no matching id here, full timeout, every time. The stub stories aren't the cost; the not-yet-built components are, regardless of whether a placeholder exists alongside the gap — and removing the stubs would give up the 🚧-sidebar-visibility and stub-everything-first workflow (step 9) for no actual speed gain.

**Fixed the real bottleneck upstream instead**: `runVisualRegression` now also fetches the _target's_ own `/index.json` once per target (mirroring how it already fetches the source's), and calls `test.skip(!targetStoryIds.has(story.id), "...not yet implemented...")` as the very first line of each test body — before opening a single browser page. A story with no target-side match now skips in well under a second instead of burning a full timeout.

**Verified with real numbers, not just that it compiles**: hand-patched the compiled fix into beam-adapter's installed copy (same approach as the Layer-decorator fix — the source lives in a TypeScript monorepo package with no isolated build available outside the full workspace, so the fastest reliable verification is patching the equivalent compiled output and running the real CLI against it) and ran `npm run adapter-tester:automated` for the **full, unfiltered 276-story suite** — previously impractical to even let finish. Result: **1m07s total, 262 skipped, 4 passed, 10 failed.** The 10 failures are real, newly-surfaced findings (Button/Layer/Loader stories exceeding the pixel threshold) — running the full suite in practical time for the first time is itself a genuine improvement, separate from the skip fix's speed win. Not chased further in this pass (out of scope — this was about fixing the tool, not auditing every component); flagged for a follow-up look, and most likely explained by the same still-unfixed Layer-decorator gap from the entry below, not yet re-verified together since the skip fix and the Layer-decorator fix haven't shipped in the same published version yet.

Restored `@chromatic-com/storybook` in `package.json`/`package-lock.json` after noticing an `npm install` during this pass had silently dropped it (unused — no addon references it anywhere — but dropping an existing devDependency as an unintended side effect of an unrelated install is worth catching and reverting, not leaving as silent drift).

Sitting on the same uncommitted branch/worktree as the Layer-decorator fix (`adapter-tester-harness-layer-decorator` in the `recursica` repo) — two related but independent fixes to the same package, both pending Matt's review/commit/publish.

### 2026-08-27 — `@recursica/adapter-tester` upgraded to 2.0 (CLI-based); found and fixed a real harness bug, not an adapter bug

Upstream refactored `adapter-tester` from hand-authored `playwright.config.ts`/`tests/visual-regression.spec.ts` (the entry directly below this one) into a real CLI (`adapter-tester`/`adapter-tester --serve`) reading an optional `adapter-tester.config.json`. Migrated: removed `adapter-tester.config.ts`, `playwright.config.ts`, `tests/visual-regression.spec.ts`, and their `tsconfig.json`/`eslint.config.mjs` entries (nothing left to type-check or lint — the CLI generates its own throwaway Playwright config at run time). Added the two `adapter-tester`/`adapter-tester:automated` scripts. No config file needed — `npm run storybook -- -p 6013` already gives the CLI everything it auto-detects.

**Investigated the 7 threshold failures from the entry below properly this time**, per `packages/adapter-tester/AGENT.md`'s explicit instruction to treat a threshold failure as "almost certainly a genuine CSS mapping bug," not something to wave off as expected cross-library variance:

- Pulled the DOM-tree JSON both `runVisualRegression` and its predecessor write per story (`/tmp/<story-id>-<target>DOM.json`) and compared `#storybook-root`'s own computed styles on both sides for `ui-kit-button--default`. Mantine's root: `height: 48px`, transparent background, zero padding — the bare button, nothing else. Beam's root: `height: 144px`, `background: rgb(252,252,252)`, `padding: 48px`, `border-radius: 24px` — a full `<Layer>` wrapper. Both real adapters' own `.storybook/preview.tsx` apply the exact same `withLayer: true, layer: 0, padding: 48px` decorator by default (confirmed by reading both files side by side) — so the asymmetry had to be coming from somewhere other than either adapter's own code.
- Found it: the throwaway Mantine harness `adapter-tester` scaffolds for the source-of-truth side (`packages/adapter-tester/src/harness/mantineSourceOfTruth.ts`'s generated `preview.tsx`) only wraps stories in `<MantineProvider>` — it never replicates the `<Layer>` decorator real mantine-adapter's own preview.tsx applies to every story. So the harness was comparing beam-adapter's properly-Layer-wrapped Button against a Mantine render with no Layer wrapper at all — a false positive baked into the test infrastructure itself, not a real difference in either adapter's token mapping. (Worth owning: this exact gap was already present in this repo's own `PROPOSAL-installed-package-harness.md` prototype that the harness was built from — the prototype's own throwaway preview.tsx never added the decorator either, so it was never verified against a Layer-wrapped story to begin with.)
- **Fixed upstream, not worked around here**: added the matching `<Layer>` decorator to the harness's generated `preview.tsx` template, on a new branch (`adapter-tester-harness-layer-decorator`) in the `recursica` monorepo — built in an isolated `git worktree` rather than the shared checkout directory to avoid clobbering unrelated uncommitted work sitting in that checkout at the time. Verified by hand-patching the fix into the already-scaffolded harness directory and re-running the exact pixelmatch comparison the tool itself does: `ui-kit-button--default` went from **10908 → 669** diff pixels (well under the 3500 threshold). Re-checked all 7 originally-failing Button/Loader stories the same way — 5 of 7 now pass outright (573–1699 diff pixels, down from 9800–15471).
- **Did not modify `diffThresholdPixels` or add any `relaxedThresholdStoryIds`** — per the AI-agent guardrail in `AGENT.md`. Not needed here regardless: the actual fix was in the test harness, not a threshold workaround.
- **The remaining 2 failures were investigated to a real conclusion, not left as "probably fine":**
  - `ui-kit-button--truncated-label` (13329 diff pixels, down from 33208): both adapters' story code are byte-identical (`<div style={{ maxWidth: "250px" }}><Button {...args} /></div>`), but Mantine's own Button renders at its full **534px** intrinsic width regardless (`max-width: none`, `display: inline-block` — confirmed via computed styles) — Mantine's Button doesn't respect a constraining ancestor's width at all, while Beam's does (Beam's own `.bm-button` sets `max-width: 100%` natively, which this adapter's CSS relies on, undisturbed). This is a real bug, but it's in **Mantine's own reference implementation**, not beam-adapter — beam-adapter's truncation is arguably more correct than the source of truth's here. Not fixed (out of scope — a different repo/package), flagged for whoever owns mantine-adapter.
  - `ui-kit-loader--layer-two-oval` (9498 diff pixels, down from 11586): partly inherent test flakiness — both sides show an actively-spinning CSS animation, frozen at an essentially random rotation angle in each independent screenshot, which two async page loads can never reliably synchronize. Separately, real signal underneath that: beam-adapter's Loader renders horizontally centered within the Layer-2 card, mantine's renders left-aligned, despite byte-identical story code — worth a follow-up look at `Loader.module.css`/`Layer`'s interaction, but not chased further in this pass.
- Re-verified `check-types`/`lint`/`build` all pass clean after the migration.

### 2026-08-27 — `@recursica/adapter-tester` brought in; found the harness works but needs real calibration (superseded above — 1.x, hand-authored config, obsolete)

`@recursica/adapter-tester` was refactored upstream from a monorepo-only tool hardcoded to Mantine-vs-MUI into a real, installable library (`defineAdapterTesterConfig`, `runVisualRegression`, `mantineSourceOfTruthWebServer` — exactly the shape proposed in `PROPOSAL-installed-package-harness.md`) and published. Wired it in following the package's own README: `adapter-tester.config.ts`, `playwright.config.ts`, `tests/visual-regression.spec.ts`, both new config files + `tests/**/*` added to `tsconfig.json`'s `include`, a `test:visual` script. Added to step 7 as scaffold-time setup, per explicit instruction.

- **Real setup bug found and fixed**: Playwright's default worker count (one per CPU core — 10 here) overwhelmed the two single-instance dev Storybook servers under concurrent page loads, causing tests to fail with plain timeouts rather than real diffs. Set `workers: 4, timeout: 60_000` explicitly in `playwright.config.ts`; confirmed fixed by re-running.
- **Real correctness bug found and fixed**: `runVisualRegression` matches stories by id between the source of truth and the target — Button/Loader's story titles were still `"Components/Button"`/`"Components/Loader"` (this repo's stub convention, see step 9), producing ids like `components-button--default` instead of Mantine's `ui-kit-button--default`. The target-side navigation then hit a nonexistent story, and the test just timed out waiting for `#storybook-root` to render — a confusing failure mode that looks identical to a hung server. Renamed both to `"UI-Kit/Button"`/`"UI-Kit/Loader"`, matching mantine-adapter exactly. Updated step 9, item 6 (turning a stub into a real component) to require this rename going forward, not just stripping the 🚧 prefix.
- **Full first-run results, Button + Loader only** (the only two real components at the time): 15 story comparisons — 4 passed, 4 timed out for a legitimate reason (Mantine has stories this adapter's don't: `PolymorphicAsLink` — Beam's Button has no polymorphism, dropped by design; Mantine's Loader story _names_ like `StaticBarsLarge` don't match this adapter's `Bars`/`Dots`/`LayerTwoOval` naming — neither is a bug to chase), 7 exceeded the inherited `diffThresholdPixels: 3500`.
- **The 7 threshold failures were investigated, not just reported**: pulled the raw screenshots (`/tmp/<story-id>-{mantine,beam}.png`, written by `runVisualRegression` itself) and the DOM ancestor chain around the button on both sides. Diagnosed — not fully root-caused — that the dominant driver looks like the global `<Layer>` decorator (both adapters' `preview.tsx` default every story to `withLayer: true`, `layer={0}`, `padding: 48px`) positioning the button differently within the fixed-size screenshot viewport rather than an actual Recursica-token color/spacing mismatch. Didn't complete a side-by-side of both adapters' full ancestor chains before concluding — flagged as unresolved, not asserted as fact. **`diffThresholdPixels: 3500` is copied from the monorepo's internal Mantine-vs-MUI config** (two UI kits with very similar default chrome) and has never been calibrated against a structurally different UI kit like Beam; don't treat a threshold failure here as an automatic "Button is broken" signal without a human actually looking at the screenshots first.
- **Not wired into `pull-request.yml`** — this is a diagnostic/calibration tool at this stage (2/15 stories comparable, the rest either don't exist yet or need a human decision on the threshold), not a CI gate. Revisit once component coverage and threshold calibration are further along.

### 2026-08-27 — `@recursica/token-analyzer` brought in (supersedes the "initial scaffold" entry's dropped-scripts note)

The "initial scaffold" entry below dropped the `analyze-tokens`/`prebuild` scripts and flagged `@recursica/token-analyzer` as an open item because it 404'd on the public npm registry at the time. **It's since been published** (confirmed `npm view @recursica/token-analyzer version` → `1.5.0`, `private: false` in the monorepo). Brought it in:

- Added `@recursica/token-analyzer@^1.5.0` to devDependencies, and restored the same `analyze-tokens`/`prebuild` scripts mantine-adapter uses verbatim: `analyze-tokens --css @recursica/official-release/recursica_variables_scoped.css --dir src/components --output token-analysis.json`, wired as `prebuild` so it runs automatically before every `npm run build` (including in CI's existing `pull-request.yml`, which already runs `npm run build` — no workflow changes needed).
- **First real run found a genuine, previously-undetected issue**: `Button.module.css`'s focus-ring rule (`.root:focus-visible`) reads four `--recursica_brand_states_focus_*` variables directly — a real "layer violation" per the analyzer's rules (component CSS must stay within the `--recursica_ui-kit_components_*` layer unless explicitly exempted), and it correctly failed the build over it. This wasn't a mistake to fix by removing the brand-layer reference — mantine-adapter's own Button does the exact same thing, for the same reason: focus/hover/disabled state tokens are documented as a global, implicit rule for every interactive element, not something each component should redefine at the ui-kit layer. Added the matching `recursica-allow-brand:` exemption header (see the analyzer's own README, or `packages/mantine-adapter/src/components/Button/Button.module.css`'s equivalent header, for the convention) instead.
- **Verified the enforcement is real, not just theoretical**: temporarily added an unexempted `var(--recursica_brand_states_hover_color)` reference to `Button.module.css`, ran `npm run build`, confirmed it printed the violation and exited non-zero (captured the real exit code directly, not through a pipe — `$?` after a `| tail` captures `tail`'s exit code, not the command being piped, a mistake worth flagging since it would have silently reported a false pass). Reverted the test reference afterward.
- **Clean bill of health otherwise**: after the exemption fix, `npm run analyze-tokens` reports zero layer violations, zero broken/missing variables, zero stale exemptions. It does report ~1543 "unused" variables across 55 component definitions — expected and non-blocking (the tool's own docs: unused variables only warn, never fail the build) — almost all of it is the 47 still-stub components, which by definition reference zero real tokens yet. Button/Loader themselves show 21 and 3 unused respectively, consistent with their documented known gaps (e.g. Button's unexposed `neutral`/`destructive` appearance variants).
- `token-analysis.json` was already correctly gitignored (line was present in `.gitignore` from the original scaffold, anticipating this) — confirmed it doesn't show up in `git status` after running the analyzer.
- Updated step 3's "confirm each dependency is actually published" bullet, which used `token-analysis` as its example of a devDependency that 404s — no longer true, removed the outdated example rather than leaving a stale claim in the process doc.

### 2026-08-26 — Loader: bars/dots implemented independently of Beam (correcting the entry below)

The entry directly below this one shipped `Loader` with only `"oval"` implemented, reasoning that Beam's `Spinner` has no bars/dots equivalent and building them from scratch would mean "inventing a design neither Beam nor Recursica's own tokens specify." Challenged directly: "we could implement our own loader using SVGs for the different types. I believe that is actually what the mantine-adapter does already."

- **Checked, not assumed**: read `@mantine/core`'s actual compiled source for its `Bars`/`Dots`/`Oval` sub-components (`Bars.mjs`, `Dots.mjs`, `Oval.mjs`, plus the compiled `Loader.css`). They are **not** SVG, and not some special native Mantine capability — they're plain `<span>` elements (three flex-item bars with `border-radius`; three circular dot spans) animated with ordinary `@keyframes` (`transform: scale()`, `opacity`, staggered `animation-delay`). mantine-adapter's `Loader.tsx` proxies `variant` to Mantine's `type` prop only because Mantine happens to build this itself internally — mantine-adapter isn't leaning on any underlying-kit capability Beam actually lacks.
- **Correction shipped**: `"bars"`/`"dots"` now render as beam-adapter's own markup — nested `<span>`s styled by `Loader.module.css`, not routed through Beam's `Spinner` at all — following the same container-size/gap/diameter ratios as Mantine's reference implementation, colored/sized from Recursica's existing per-size `indicator-color`/`size`/`thickness-size` Loader tokens directly (no `--bm-comp-*` indirection needed since no Beam component is involved). The dev-mode `console.warn` fallback was removed. `"oval"` is unchanged — still routed through Beam's own `Spinner`, since that's a real already-themed native element with no reason to reinvent it.
- **Real remaining gap, now narrower and explicit**: Recursica's Loader tokens still don't define bar/dot-specific geometry (bar border-radius, dot diameter ratio, gap ratio) — only the generic per-size tokens shared with `"oval"`. The exact proportions used are this adapter's own derivation (matching Mantine's ratios), not literally spec'd by Recursica's tokens yet.
- **Build-time gotcha hit and fixed**: the project's `recursica-postcss-vars` PostCSS plugin validates every `var(--recursica*)` reference against `recursica_variables_scoped.css` — including locally-defined intermediate custom properties that merely happen to start with the same prefix, not just actual design tokens (confirmed by reading the plugin's source: it matches on string prefix `--recursica`, it doesn't distinguish "defined in this same file" from "must exist in the tokens file"). The local intermediate variables added for bars/dots (`--recursica-loader-size` etc.) were renamed to `--loader-*` (unprefixed, Mantine-style) to avoid tripping it — `npm run build` failed with a clear `Missing variable` error before the rename, passed clean after.
- Verified with real computed styles via Playwright against a freshly booted Storybook (not just "it renders"): both new variants show 3 children, correct `background`/`border-radius` per variant, a distinct running `animationName` per variant, at the token-driven indicator color — re-verified again after the `--loader-*` rename to confirm the CSS Modules class/keyframe hashes still resolved correctly. Confirmed no regression to Button's loading-spinner color behavior (still the button's own fg color, per the entry below). `check-types`/`lint`/`build` all pass clean. Changeset and `IMPLEMENTATION_NOTES.md`/`USAGE.md`/stories updated to remove the old "only oval is implemented" framing.
- **Process note**: this work was done directly on `main`'s working tree (uncommitted, left over from the prior pass) — caught before any further work, moved onto a proper `loader-component` branch off `main`. Matt still does all committing/review per the standing rule; nothing was committed here either.

### 2026-08-26 — Loader: second real component, integrated into Button

Replaced the `Loader` stub with a real implementation wrapping `@viasat/beam-react`'s `Spinner`, then integrated it into `Button`'s loading state per explicit instruction ("this should be part of our first step of implementing the button" — done as a follow-up pass since `Loader` didn't exist yet when `Button` was first built).

- **Beam's Spinner has no bars/dots equivalent** — confirmed via `Spinner.d.ts` (no `variant`/`type` prop) and its compiled CSS (`SpinnerWrap.css`, a single circular SVG design). `RecursicaLoaderProps` still accepts `"oval" | "bars" | "dots"` for cross-adapter type compatibility, but this adapter only renders `"oval"`, with a dev-mode `console.warn` when a caller requests the other two. Recursica's own design tokens don't specify bars/dots geometry either, so this isn't something to improvise from scratch — flagged as an open item.
- **`className` doesn't reach Beam's `Spinner`** — confirmed in its compiled source (`{ className: _className, ...props }`, explicitly discarded). Unlike `Button`, `Loader` wraps `<BeamSpinner>` in its own `<span className={styles.root}>` so the CSS custom-property override mechanism still has an anchor element; the mechanism itself (override Beam's `--bm-comp-spinner-*` variables) is otherwise identical to Button's pattern.
- **No injection point exists to make Button render the actual `<Loader>` component** — confirmed via Beam's compiled `Button.es.js`: Beam's `Button` renders its internal spinner entirely internally with no `loaderProps`/render-prop equivalent to substitute a different element, unlike Mantine's Button (which is how mantine-adapter's `Button` injects its own `<Loader />`). Worked around via CSS cascade instead: `Button.module.css` overrides the same spinner-_size_ variables `Loader.module.css` overrides, scoped under Button's own `.root` — Beam's internal spinner is a DOM descendant of `.root`, so the override reaches it without literally being the `<Loader>` React component.
- **Found and fixed a real bug via computed-style verification, not assumption**: first attempt also overrode the spinner's _color_ the same way as size. Checking the actual rendered `stroke` value showed it had no effect — Beam's own `Button.css` has a more specific rule tying the internal spinner's color to the button's own foreground color per variant, which never reads the generic spinner color variable in a button context. Removed the dead override. **This turned out to be correct/desirable behavior, not a gap**: the loading spinner already automatically matches each button variant's own (already Recursica-token-driven) text color — arguably better than the genesis adapter, where mantine-adapter's injected `<Loader>` always uses a fixed indicator color regardless of button variant, which mantine-adapter's own docs flag as a real contrast risk. Full detail in both `Button/IMPLEMENTATION_NOTES.md` and `Loader/IMPLEMENTATION_NOTES.md`.
- **`useRecursicaLoader` now does something real** (previously accepted-and-discarded since `Loader` didn't exist yet) — gates whether Button's spinner-size override applies, via a new `data-recursica-loader` attribute on `.root`. `loaderVariant`/`loaderSize` remain non-functional inside `Button` for the same reasons `Loader` itself can't act on them (only one visual style exists; spinner size ties to Button's own `size` with no independent control found in Beam's compiled source) — both stay in the type for cross-adapter compatibility.
- Verified end-to-end with real computed styles (not just "it renders"): standalone `Loader` at three sizes, `Button`'s loading spinner size with `useRecursicaLoader` true vs. false (confirmed genuinely different), and the color-override dead-code finding itself was only caught this way. `llms.txt` updated, changeset added. `check-types`/`lint`/`build` all pass clean, re-verified with a fresh Storybook boot after the fix.

### 2026-08-26 — new step 8 (styling system decision) + `docs/STYLING_SYSTEM.md`

Explicit instruction: before any component CSS gets written, an adapter needs an upfront, written decision on how it styles Recursica components on top of the underlying UI kit — not something to work out ad hoc on the first component and leave scattered across that component's own implementation notes. Added a new **step 8** to this doc ("Decide and document the styling system"), renumbering the former steps 8–10 to 9–11 (stub components, build components, governance) and fixing every internal `step N` cross-reference in this file accordingly.

The new step requires answering, in writing, in `docs/STYLING_SYSTEM.md`: (1) how the underlying kit styles itself (read its compiled output, not its docs), (2) how Recursica's own overrides avoid polluting global styles / bleeding into a plain, unwrapped instance of the same underlying component, (3) how Recursica's own styling stays hard to target/override from outside (scoped, unique class generation — CSS Modules is the default answer, not a hard requirement), (4) preference for the underlying kit's own idiomatic approach but for **static** styling over CSS-in-JS when there's a choice, and (5) a concrete conclusion on the overstyling-prevention mechanism (both the override technique and how it composes with the TypeScript/runtime blocking layers).

**For beam-adapter specifically**: wrote `docs/STYLING_SYSTEM.md` now, retroactively — the research itself was already done while building Button (see the entry below), just not written up as its own upfront doc. Conclusion matches what Button already implements: Beam is plain-CSS/custom-property-driven (no CSS-in-JS, confirmed by reading compiled source), so this adapter overrides Beam's own `--bm-comp-*` variables scoped to `.root` rather than fighting class specificity the way the genesis adapter's direct-property-override approach does — this differs from mantine-adapter and is called out explicitly as the one place beam-adapter's approach diverges. `CONTRIBUTING.md`'s required-reading list updated to include it (now item 2, before the component-dev-guide pointer). Every future component's `IMPLEMENTATION_NOTES.md` should defer to this doc for the _why_ of the styling mechanism rather than re-explaining it per component, the way Button's currently does (Button was built before this doc existed).

### 2026-08-26 — formalized the prop-audit checklist (step 10.1); fixed a gap it caught in Button

Explicit instruction: every component's implementation should start with an audit of the underlying UI-kit component's full prop list against the canonical `Recursica<Name>Props` contract — appearance-affecting props not in the contract get removed (type + hardcoded default + runtime `UNSUPPORTED_PROPS` deletion, all three), behavior-affecting props with no Recursica equivalent get a documented allow-or-block decision each, and Recursica's own props always take precedence over the raw prop they map to. Added as step 10's new item 1 in this doc (see above) — this is a per-adapter-agnostic process point, not Beam-specific, so it lives in the shared step 10, not `docs/COMPONENT_DEV_GUIDE.md`.

Running this checklist against the just-built Button found a real gap: `appearance` and `kind` (Beam's raw props) were `Omit<>`'d from the public type and hardcoded in the render, but **not** in `UNSUPPORTED_PROPS` — meaning a plain-JavaScript caller forcing them would only have been stopped by JSX assertion order (our `appearance="accent"` happening to come after `{...sanitizedProps}`), not by explicit deletion. Same gap for `iconBefore`/`iconAfter`/`iconOnly`. Fixed: all five added to `UNSUPPORTED_PROPS`. Verified the fix matters, not just that the code compiles: forced `appearance=destructive;kind=bare` through a Storybook story's `args` and confirmed the rendered class stayed `bm-button--accent-filled`. `width`/`fluid`/`theme`/`productType` were already correctly handled from the first pass. See `Button/IMPLEMENTATION_NOTES.md`'s updated "appearance is not exposed" section for the full detail.

### 2026-08-26 — Button: first real component implementation

Replaced the stub (`src/components/Button/`) with a real implementation wrapping `@viasat/beam-react`'s `Button`. This is the template for every component after it — the key discovery here (CSS custom-property overrides) applies broadly, not just to Button.

- **Beam is entirely CSS-custom-property-driven** — confirmed by reading the compiled `Button.es.js`/`Button.css` directly: `.bm-button--accent-filled { background: var(--bm-comp-btn-color-accent-filled-bg); ... }`, no hardcoded values anywhere. So `Button.module.css` overrides Beam's own `--bm-comp-btn-*` variables on `.root` (mapped to `--recursica_ui-kit_components_button_*` tokens) instead of fighting Beam's class-selector specificity with direct property overrides the way mantine-adapter's CSS does. This sidesteps stylesheet-import-order concerns entirely — see `Button/IMPLEMENTATION_NOTES.md`'s "Beam is CSS-custom-property-driven" section for the full reasoning. **Check for a component's own `--bm-comp-*` variables before reaching for direct property overrides on future components.**
- **`appearance` (Beam's accent/neutral/neutral-subtle/destructive dimension) is not exposed** — `RecursicaButtonProps` (canonical, `@recursica/adapter-common`) only has `variant`, mapped to Beam's `kind`. Hardcoded `appearance="accent"` (Beam's own default) rather than inventing a beam-adapter-only prop that would break "same prop surface across every adapter." Only the `accent` variable family is overridden in the CSS; `neutral`/`destructive` are untouched Beam defaults.
- **No custom Loader injection** — mantine-adapter's Button injects its own Recursica-styled `<Loader />` on `loading`. Beam's Button already renders its own token-driven spinner natively; re-theming that (via the same custom-property-override pattern, most likely) is scoped to when the real `Loader` component gets built, not duplicated ad hoc inside Button. `loaderVariant`/`loaderSize`/`useRecursicaLoader` stay in the type (cross-adapter compatibility) but are unused.
- **No polymorphism** — Beam's `Button` is a plain `forwardRef` component, confirmed via its `.d.ts` (`React.ForwardRefExoticComponent<...>`, no `component`/`renderRoot`-equivalent prop). mantine-adapter's `PolymorphicAsLink` story has no Beam equivalent and was dropped from the adapted story set rather than faked.
- **Icon handling is simpler than Mantine's** — Beam's `iconBefore` is already auto-wrapped in its own `bm-button__icon-wrap` span and Beam exposes an explicit `iconOnly` boolean, so no manual wrapper-span styling hack was needed (mantine-adapter needs one because Mantine doesn't do this itself).
- **Stories adapted from the genesis adapter's `Button.stories.tsx`**, dropping only `PolymorphicAsLink` (unsupported) — same story names/args otherwise, so visual/behavioral parity is directly comparable component-by-component with mantine-adapter's Storybook.
- **Verified with real computed-style assertions, not just "it renders"**: booted Storybook fresh, used Playwright to read `getComputedStyle()` on the actual rendered `.bm-button` element across multiple stories — confirmed `backgroundColor`/`color`/`outlineColor`/`borderRadius`/`height`/`fontFamily` resolve to the expected Recursica token values (not Beam's own defaults) for solid/outline/text variants and both sizes, confirmed disabled opacity (`0.38`) and cursor (`not-allowed`) apply correctly. Also functionally verified the overstyling requirement directly: `className`/`style` passed without `overStyled` do **not** reach the DOM (confirmed via Storybook's `args` URL parameter + a live computed-style/class check), and **do** reach it with `overStyled={true}` — not assumed from `filterStylingProps` working elsewhere, checked on this specific component.
- **Known simplifications, documented not hidden**: icon color relies on `currentColor` inheritance rather than a dedicated per-variant token; border radius maps to Recursica's "label" content-type radius (Beam has no separate icon-only/icon-label radius concept); Beam's inner label padding (`--bm-comp-btn-space-label-x`) is left at its default. Full list and rationale in `Button/IMPLEMENTATION_NOTES.md`.
- Added `llms.txt`'s Button entry, and a changeset (`.changeset/button-component.md`, minor bump) per `CONTRIBUTING.md`'s process.

### 2026-08-26 — removed the test infrastructure (`npm run test`, CI test step) — explicit instruction, not a bug fix

**Supersedes the two entries directly below** (the Playwright CI-gap fix, and the `--passWithNoTests` bit of the stub-components entry). Both of those were fixing a `test` script and CI test step that this entry now removes entirely — not because the fixes were wrong, but because the thing they were fixing shouldn't have existed in this repo in the first place.

While answering an unrelated question ("does the genesis adapter run these Storybook tests?"), checked `mantine-adapter`'s actual `package.json` directly: it has **no `"test"` script at all** — scripts are just `dev`, `build`, `build-storybook`, `lint`, `storybook`, `analyze-tokens`, `prebuild`. The monorepo's root `pull-request.yml` runs `npm run test` (→ `turbo run test`), and Turborepo silently skips any workspace without a matching script — so mantine-adapter's `vitest.workspace.ts`/`@storybook/addon-vitest`/`playwright` setup, despite being present in its own `package.json`/config files (and therefore faithfully copied into this repo per step 4's "copy as-is" instruction for those files), **never actually runs in the genesis adapter's CI**. It's present-but-dead infrastructure there.

Across the earlier passes in this log, a `"test": "vitest run"` script and a CI test step got added to _this_ repo anyway — going beyond what copying the genesis adapter's files actually implied, without being asked to. Explicitly instructed to remove all of it. Removed:

- `package.json`: the `"test"` script, and `npm run test` from the `precommit` script.
- `scripts/lint-staged.config.cjs`: the `npm run test` task.
- `.github/workflows/pull-request.yml`: the `Install Playwright browser` and `Test` steps entirely.

**Not removed** (matches the genesis adapter, which has these too, just unused): the `vitest`/`@storybook/addon-vitest`/`@vitest/browser`/`@vitest/coverage-v8`/`playwright` devDependencies, `vitest.workspace.ts`, `.storybook/vitest.setup.ts`, `vitest.shims.d.ts`. Also not removed: the 49 `.stories.tsx` files themselves — those are real Storybook stories (the actual browsing surface), unrelated to whether anything automatically test-runs them.

**Also surfaced, not yet acted on**: the genesis adapter likewise has no `"check-types"` script, meaning `packages/mantine-adapter`'s TypeScript may not be type-checked in CI either via the same Turborepo-skip mechanism — this repo _does_ have a `check-types` script and CI step, which is the same kind of "added beyond the genesis adapter" situation as `test` was. Left as-is pending an explicit decision, rather than assumed into the same removal.

**Lesson**: "copy the genesis adapter's config files" (step 4) does not mean "also wire up whatever infrastructure those files imply, if the genesis adapter itself doesn't." When a file is present-but-unused in the genesis adapter, replicate that state — present-but-unused — rather than "completing" it into something the genesis adapter doesn't actually do. If turning dead infrastructure into a live one seems like a good idea, ask first.

### 2026-08-26 — `pull-request.yml` was missing the Playwright browser install (CI-breaking gap, caught late)

`npm run test` failed in real CI with `browserType.launch: Executable doesn't exist at .../chromium_headless_shell-.../chrome-headless-shell`. This was a genuine gap, not a new regression: the very first stub-components pass (see that entry above) already documented that `npx playwright install chromium` is required before `npm run test` can run — but that requirement was only ever run manually in local verification, never actually added as a step in `.github/workflows/pull-request.yml`. Local `npm run check-types`/`lint`/`build`/`test` all stayed green through every subsequent change because the browser binary happened to already be cached locally from that first manual install — CI starts from nothing every run, so it never had one.

**Fixed**: added an `Install Playwright browser` step (`npx playwright install --with-deps chromium`, `--with-deps` because Ubuntu CI runners need OS-level libraries a bare `npx playwright install` doesn't pull in — not needed on macOS, which is why this wasn't caught locally) between `Build` and `Test` in `pull-request.yml`.

**Verified the actual failure and the actual fix, not just re-read the workflow file**: moved `~/Library/Caches/ms-playwright` out of the way locally to simulate a browser-less fresh CI environment, reproduced the identical error and a non-zero exit code from `npm run test`, then re-ran `npx playwright install chromium` and confirmed all 49 tests pass and exit 0. **Lesson for future adapters following this process**: a new required setup step discovered while verifying locally (step 7's "boot Storybook, run the tests, don't just build") needs to be added to the CI workflow in the same pass, not just noted in the decisions log — otherwise CI silently diverges from what's been "verified" locally the moment the CI environment starts from a clean cache.

### 2026-08-26 — CSS Modules wired in; audited Beam's real overstyling surface

Two changes, both prompted by needing to actually understand how Beam handles styling before any real component gets built.

- **Every stub component now has a `{Name}.module.css`** (empty, header comment only) imported and applied via `className={styles.root}` in its `.tsx` file — see the updated step 9 above for the convention. Generated via the same kind of one-off Node script as the original 49 stubs, not by hand. Re-verified after: `check-types`, `lint`, `build` (103 modules now, up from 54, since each stub imports its own CSS module), and all 49 portable-stories tests still pass.
- **Audited `@viasat/beam-react@2.58.1`'s actual `.d.ts` files** (`Button.d.ts`, `TextField.d.ts`, and a grep across all 57 files in `lib/`) to answer "how does Beam allow overstyling, and does `filterStylingProps` actually block it" — not guessed:
  - Confirmed `className`/`style` are the _only_ generic overstyling vector: every sampled component extends React's native `ComponentPropsWithoutRef`/`ComponentPropsWithRef`, and no `sx`/`css`/`classes`/`classNames`-style richer API exists anywhere in the library. `BLOCKED_STYLING_KEYS = ["className", "style"]` (already the value since the initial scaffold) is therefore already the _complete_ block list for this UI kit — nothing was actually missing here, but this is now a verified fact instead of a placeholder guess.
  - Found and populated Beam's real margin-prop vocabulary into `LAYOUT_PROPS` (previously empty): `m`, `mx`, `my`, `mTop`, `mBottom`, `mBefore`, `mAfter` (Beam's own naming — not Mantine's `mt`/`mb`/`ml`/`mr`) plus `gap`, sourced from `@viasat/beam-react/utils/spacing`'s `MarginSpacingProps`/`MARGIN_PROP_NAMES` (confirmed present on `Button`; not yet confirmed on other components). These stay _allowed_ (not blocked) per `OVERSTYLING.md`'s margin-vs-padding distinction. Beam also defines a `PaddingSpacingProps` mixin (`p`, `px`, `py`, `pTop`, `pBottom`, `pBefore`, `pAfter`) that **no component currently uses** — left unhandled (neither blocked nor allowed) since there's nothing to block yet; flagged in `docs/COMPONENT_DEV_GUIDE.md` to revisit if a component ever exposes it (it would go in `BLOCKED_STYLING_KEYS`, being internal-not-layout styling, per the philosophy).
  - `docs/COMPONENT_DEV_GUIDE.md`'s "Beam-specific styling notes" section (previously a placeholder list of things to figure out later) is now filled in with these findings, plus corrected the stale claim that Beam's theming "depends on `next-themes`" as the public API — it's `BeamThemeProvider`, already resolved and documented in `SETUP.md` from an earlier pass.
- **`CONTRIBUTING.md`'s `critical_agent_directive` now explicitly states the CSS Modules requirement** as its own numbered point (previously only implied via a "read `docs/COMPONENT_DEV_GUIDE.md`" pointer) — a contributor reading only `CONTRIBUTING.md`'s top block shouldn't have to open a second file to learn styling is CSS-Modules-only.

### 2026-08-26 — `scripts/lint-staged.config.js` → `.cjs` (real commit-blocking bug)

Found via an actual failed `git commit`: Husky's pre-commit hook failed outright with `Failed to read config from file "scripts/lint-staged.config.js"` / `No valid configuration found`. Root cause: this package's `package.json` has `"type": "module"` (needed for the rest of the ESM tooling — `vite.config.ts`, `.storybook/*.ts`), but `scripts/lint-staged.config.js` used CommonJS `module.exports = {...}`. Node parses a plain `.js` file as ESM when the nearest `package.json` says `"type": "module"`, so `module.exports` isn't valid there — the monorepo's own `scripts/lint-staged.config.js` gets away with this because the monorepo's root `package.json` has no `"type"` field at all (defaults to CommonJS). **Fixed**: renamed to `scripts/lint-staged.config.cjs` (the `.cjs` extension is always CommonJS regardless of `package.json`'s `"type"`) and updated `.husky/pre-commit`'s `--config` flag to match. Verified by actually invoking `npx lint-staged --config scripts/lint-staged.config.cjs` directly (not just re-reading the file) — config loads and the full task list (prettier, eslint, check-types, test) runs to completion.

**Generalize this**: any standalone adapter with `"type": "module"` in `package.json` needs `.cjs` for `scripts/lint-staged.config.js` (and any other config file relying on `module.exports`/`require`) — this isn't specific to Beam, it'll hit every future adapter built from this same step-4 Husky/lint-staged setup unless the guidance is updated. **Step 4's instructions above are now updated to say `.cjs` directly**, rather than leaving this as a trap the next adapter has to rediscover.

### 2026-08-26 — generated theme files are checked in AND are the source of truth the build reads from

Two-part policy reversal from the initial-scaffold entry below (which gitignored the four theme files, reasoning they were consumer-convenience artifacts unused by this repo's own build). Corrected per explicit direction:

1. **The expected workflow is `@recursica/official-release`'s postinstall running once at repo init, and its output — `recursica.json`, `recursica_variables_scoped.css`, `recursica_brand.json`, `recursica_tokens.json`, `recursica_ui-kit.json` — being committed**, not ignored. `.gitignore`'s four-file block was removed entirely.
2. **`@recursica/official-release` is a bootstrap-only dependency, not a runtime path the build resolves against.** `vite.config.ts` and `.storybook/main.ts`/`preview.tsx` were changed from `require.resolve("@recursica/official-release/recursica_variables_scoped.css")` / `import "@recursica/official-release/recursica_tokens.json"` (node_modules) to plain relative paths (`resolve(__dirname, "../recursica_variables_scoped.css")`, `import "../recursica_tokens.json"`) pointing at the checked-in root copies. The mental model: a new adapter's developer runs `npm install` once to bootstrap these files into existence, and from then on **the root copies are what the project is built against and what gets updated going forward** (by hand, or via a Forge Recursica export per `SETUP.md`) — `@recursica/official-release` isn't consulted again after that first install.

Together, these mean the committed root files are provably what Storybook/the build actually read, not two copies (`node_modules` vs. root) that are merely expected to match.

**One real bug caught fixing this**: `.storybook/main.ts` is loaded directly by Storybook under Node's native ESM loader — unlike `vite.config.ts`, which Vite's own config loader shims `__dirname` into — so a bare `resolve(__dirname, ...)` there throws `ReferenceError: __dirname is not defined in ES module scope` and Storybook fails to boot entirely (not a subtle runtime issue — the dev server won't start). Fixed by reconstructing `__dirname` the standard ESM way (`dirname(fileURLToPath(import.meta.url))`), matching the pattern `eslint.config.mjs` already used. Caught by actually booting Storybook and checking the log, not just `check-types`/`build`, which both stayed green through the broken version — this is a good example of why step 7's "boot Storybook, not just build" verification matters.

Verified after the fix: fresh-cache Storybook boot succeeds, a story's network requests show `recursica_variables_scoped.css` being fetched from the project root (`http://localhost:<port>/recursica_variables_scoped.css`, not `/node_modules/...`), and all 49 stub component tests still pass.

### 2026-08-26 — stub components (49)

First pass at step 9, before any real component implementation: a placeholder for every component, so the full surface is importable, browsable in Storybook, and type-checked, and so building real components from here on is "fill in one file at a time" rather than "also wire up scaffolding as you go."

- **Component list**: 49 components, one per top-level folder in the genesis adapter's (`mantine-adapter`) `src/components/`, alphabetically: Accordion, AssistiveElement, AutoComplete, Avatar, Badge, Breadcrumb, Button, Card, Checkbox, Chip, Container, DatePicker, Dropdown, FileInput, FileUpload, Flex, FormControlLayout, FormControlWrapper, Grid, Group, HoverCard, Label, Link, Loader, Menu, Modal, NumberInput, Pagination, Panel, Popover, Radio, ReadOnlyField, SegmentedControl, Slider, Stack, Stepper, Switch, Table, Tabs, Text, TextArea, TextField, TimePicker, Timeline, Title, Toast, Tooltip, TransferList, Tree. Excludes `RecursicaThemeProvider` and `Layer` — mantine-adapter's own folders for those contain only a `USAGE.md`; the real implementation lives in `@recursica/adapter-common` and is already available via `export * from "@recursica/adapter-common"` in `src/index.ts`. Also excludes sub-components (`CheckboxGroup`, `RadioGroup`, `SwitchGroup`, `AccordionItem`/`AccordionControl`/`AccordionPanel`, `TimelineItem`, `PanelFooter`, `GridCol`) and the canonical `RECURSICA_COMPONENTS` const's `Box`/`EmptyValueRenderer`/`Typography`/`Autocomplete` entries (that array has drifted from mantine-adapter's actual naming/set — followed the real genesis adapter folder list per the instruction to stub "the components in the mantine adapter," not the const array) — those are follow-up work once their parent/sibling component is actually built for real.
- **Structure**: each component is `src/components/<Name>/<Name>.tsx` + `<Name>.stories.tsx` (no per-folder `index.ts` — standardized on `src/components/index.ts` exporting `"./<Name>/<Name>"` directly, since mantine-adapter's own folders mix both patterns inconsistently). No `.module.css` yet — nothing to style until there's a real implementation.
- **Shared `_InDevelopmentStub.tsx`**: every stub component's body is a one-line call to a single shared placeholder component (`src/components/_InDevelopmentStub.tsx`, leading underscore = not itself part of the public component set) that renders `"{componentName}: In development"`. Chosen over duplicating the placeholder markup/text 49 times so the copy, styling, and behavior only need to change in one place; each component's own file still carries its own real `<Name>Props` type (`RecursicaOverStyled<{ children?: React.ReactNode }>` for now) so the public API shape is component-specific even though the body isn't yet.
- **`src/index.ts` rewritten to match the genesis adapter's real pattern**: previously this was a blanket `export * from "./components"`. Replaced with an explicit `import * as rawComponents from "./components"` + one `export const <Name> = wrapComponent(rawComponents.<Name>) as typeof rawComponents.<Name>;` line per component (mirroring `mantine-adapter/src/index.ts` exactly), plus an explicit `export type { ...Props }` block. **Simplification vs. the genesis adapter**: mantine-adapter leaves its polymorphic primitives (`Flex`, `Grid`, `Group`, `Stack`, `Container`, `Link`, `Text`) unwrapped (exported raw, not through `wrapComponent`) to preserve polymorphic typing. All 49 stubs here — including those seven — go through `wrapComponent` uniformly for generation simplicity. **Revisit this when those seven are implemented for real** — if they need `createPolymorphicComponent`-equivalent typing, they'll likely need to go back to being unwrapped, matching the genesis adapter.
- **`eslint.config.mjs` diverges from the genesis adapter**: added `"@typescript-eslint/no-unused-vars": ["error", { argsIgnorePattern: "^_" }]`, which mantine-adapter's config doesn't have. Needed because every stub component takes a typed-but-ignored `_props` parameter (to carry the real prop type without reading it yet); the default rule doesn't exempt underscore-prefixed args. Standard, common ESLint convention — not expected to cause problems once components stop ignoring their props.
- **`npm run test`'s `--passWithNoTests` flag removed** from `package.json` now that real story files exist — planned in the initial-scaffold entry above. Also required `npx playwright install chromium` (browser binaries aren't installed by `npm install` alone) before `@storybook/addon-vitest`'s portable-stories tests could actually run.
- **Verified, not just built**: `npm run test` ran all 49 stories through `@storybook/addon-vitest`'s real headless-Chromium portable-stories runner — 49/49 passed (each one actually mounts and renders without throwing, using the real `BeamThemeProvider`/CSS wiring from the previous pass). `npm run storybook`'s story index (`/index.json`) lists all 49 `Components/<Name>` entries. `check-types`, `lint`, and `build` (54 modules, dist output unchanged in shape) all pass clean.
- **Generated via a script, not by hand**: given the volume (49 × 2 files + 3 rewritten files), used a one-off Node generator script (not committed to the repo) to produce all the above from templates, rather than authoring each file individually — worth knowing if the actual file contents ever look suspiciously uniform, that's why.
- **🚧 marker added to every stub's story title** (`title: "Components/🚧 <Name>"`) after the fact, so in-development components are visually obvious in the Storybook sidebar without any extra tooling — Storybook renders the emoji as part of the title text as-is. Verified via Storybook's own story index (`/index.json`): all 49 stub entries carry the `🚧`, and no other entries (e.g. the default token/theme demo stories that started showing up once `@recursica/storybook-template` was bumped past the `stories`-files-array fix — see the earlier upstream-bug write-up) do. Documented as a required part of the step-9 stub process going forward, including removing it in step 10 once a component is implemented for real.
- **CJS/ESM interop crashes ("does not provide an export named 'default'") hit `@recursica/storybook-template`'s own bonus default demo stories after its `stories`-files-array fix landed** (0.7.7) — reproduced with Playwright against a real running Storybook instance, not guessed. Two distinct symptoms, both scoped to the _browser_ runtime, not `npm run build`/`test`:
  - `semver` — reproduced on the auto-generated "Docs" tab for any `autodocs`-tagged story (e.g. `Theme/Dimensions`'s Docs tab). **Fixed here**: extended `config.viteFinal` in `.storybook/main.ts` to add `semver` to Vite's `optimizeDeps.include`, forcing Vite to pre-bundle (and therefore CJS-interop-wrap) it instead of serving it raw when reached indirectly through `storybook`'s `manager-api` package. Re-verified with Playwright after a full cache clear (`node_modules/.cache/storybook`, `node_modules/.vite*`) — error gone.
  - `use-sync-external-store/shim` — reproduced specifically on stories that call `@recursica/storybook-template`'s `useRecursicaJson()` hook (`Theme/Dimensions`, `Tokens/Color`; confirmed **not** present on `UI-Kit/Layer`, which doesn't use that hook). The same `optimizeDeps.include` approach did **not** fix this one — added it anyway (harmless) but it's still open. **This adapter's own 49 stub component stories don't use `useRecursicaJson` and are unaffected** — reproduced clean with Playwright against `components-🚧-accordion--default` and `components-🚧-card--default` via the exact same full-app URL pattern (`/?path=/story/<id>`) that reproduces the bug on the bonus stories. ~~**Not resolved** — looks like it needs a fix inside `@recursica/storybook-template`'s `useRecursicaJson` implementation (or upstream in `storybook` itself), not something a per-project `optimizeDeps` override can fully reach. Flagged for the repo owner; doesn't block building real components here.~~ **Resolved 2026-08-28 — see the entry near the top of this log.** Not a `storybook-template`-internal bug after all: this project's own `.storybook/preview.tsx` was importing `createPreviewConfig` from the `/preview` subpath while the demo stories import `useRecursicaJson` from the package root, and Vite's dependency optimizer was duplicating the shared `React.createContext()` singleton across those two separate entry points. Fixed by importing from the package root in both places.

### 2026-08-26 — initial scaffold

- **Package name**: `@recursica/adapter-beam`, standalone repo `borderux/recursica-adapter-beam` (same GitHub org as the `recursica` monorepo, but not a subpackage of it).
- **Peer deps**: `@viasat/beam-react@^2.58.1`, `react`/`react-dom` matching Beam's own peer range (`^16.14.0 || ^17.0.0 || ^18.0.0 || ^19.0.0`). Confirmed via `npm view @viasat/beam-react peerDependencies`. Beam bundles its own fonts/shared styles/tokens (`@viasat/beam-fonts`, `@viasat/beam-shared`, `@viasat/beam-styles`, `@viasat/beam-tokens`) as regular dependencies of `beam-react` itself, not peers.
- **Beam's export map**: `@viasat/beam-react` publishes deep per-component subpath exports (`./*` → `lib/*/index.{es,cjs}.js`) in addition to the root barrel. It also has its own `./styles.css` export (`assets/index.css`, a bundled copy of the token CSS), but — verified against the installed package's own `README.md` — that's **not** what Beam's docs recommend importing. The documented pattern is `@viasat/beam-tokens/styles.css` + `@viasat/beam-fonts/styles.css` (see the theme/provider entry below); `SETUP.md` follows that, not the `./styles.css` export.
- **Dropped `@repo/typescript-config`** from devDependencies — confirmed via `npm view` it 404s (monorepo-internal, unpublished), and the genesis adapter's own `tsconfig.json` doesn't actually extend it, so it was dead weight.
- ~~**Dropped the `analyze-tokens`/`prebuild` scripts** — they depend on `@recursica/token-analyzer`, which also 404s on the public registry ("Internal CLI tool" per its own description). **Open item**: either the `recursica` org needs to publish `@recursica/token-analyzer` publicly, or standalone adapters permanently do without automated token-usage analysis. Not resolved here — flagged for the repo owner.~~ **Superseded 2026-08-27 — see the entry above: `@recursica/token-analyzer` has since been published and is now wired in.**
- **Canonical doc links**: `docs/COMPONENT_DEV_GUIDE.md` and `docs/COMPONENT_STORYBOOK_GUIDE.md` link to `adapter-common`'s canonical guides via `https://github.com/borderux/recursica/blob/main/packages/adapter-common/docs/...` rather than a relative path, since `adapter-common`'s own npm package only ships `dist`, `llms.txt`, and `ARCHITECTURE.md` — the `docs/` folder isn't published and doesn't exist in this repo.
- **Storybook port**: `6013` (the genesis adapter, `mantine-adapter`, uses `6011`).
- **Beam theme/provider wiring — resolved.** Verified against the installed `@viasat/beam-react@2.58.1` package's own `README.md` and `.d.ts` files (not guessed): Beam's provider is `BeamThemeProvider` (exported from the package root — `import { BeamThemeProvider } from "@viasat/beam-react"`), taking a `defaultTheme?: Partial<{ mode: "light" | "dark"; accent: Accent; productType: "enterprise" | "consumer" }>` prop (defaults: `mode: "light"`, `accent: "teal"`, `productType: "enterprise"`), plus `forcedMode` and `enableSystemMode`. It sets a `data-bm-theme` attribute on the DOM (parallel to `RecursicaThemeProvider`'s `data-recursica-theme`). `.storybook/preview.tsx` now wraps every story in `<BeamThemeProvider defaultTheme={{ mode: "light" }}>` (outside the existing `<Layer>` decorator), and imports `@viasat/beam-tokens/styles.css` + `@viasat/beam-fonts/styles.css` — confirmed resolving correctly via Vite's dev server (`@viasat/beam-tokens/styles.css` maps to `tokens.css` through that package's own export alias). `SETUP.md` documents the same two-provider pattern (`<BeamThemeProvider><RecursicaThemeProvider>`) for consuming applications. `accent`/`productType` are Beam-specific theming concepts with no Recursica equivalent yet — left as an open question for whoever builds the first component to decide whether/how to expose them through the adapter's own API, rather than resolved here.
- **`npm run test` (`vitest run`) exits non-zero on an empty component barrel** — with zero `.stories.tsx`/`.test.ts` files, Vitest's default behavior is "no test files found, exiting with code 1," which would make `pull-request.yml` fail permanently until the first component lands. Added `--passWithNoTests` to the `test` script as a temporary accommodation; **remove that flag once the first component's story/test exists**, since at that point a "no tests found" result would be a real regression worth CI catching, not a legitimate empty-scaffold state.
- **`vite-plugin-dts`'s `rollupTypes: true` — resolved 2026-09-10, was never actually about "zero components".** `npm run build` threw `[vite:dts] Internal Error: Cannot assign isExternal=false for the symbol RecursicaLabelProps because it was previously registered with isExternal=true` (an api-extractor internal error) whenever `rollupTypes: true` was combined with `bundledPackages: ["@recursica/adapter-common"]`. The original entry here blamed "zero local components" and deferred the fix — wrong: re-verified it still reproduces with all 53 real components built, on a from-scratch `npm ci`, with a minimal 2-export barrel. Root-caused by patching `@microsoft/api-extractor`'s `AstSymbolTable`/`ExportAnalyzer` with temporary diagnostic logging (not guessed): `RecursicaLabelProps` gets fetched twice for the same declaration in `@recursica/adapter-common/dist/src/types.d.ts` — once via `Label.tsx`'s own `import { type RecursicaLabelProps } from "@recursica/adapter-common"` (classified `isExternal=true`), once via `components/index.ts`'s `export * from "@recursica/adapter-common"` combined with `bundledPackages` (classified `isExternal=false`) — and `bundledPackages`'s `packageId.name` match only fires for the second path, not the first, so the two classifications collide. Confirmed via the same instrumentation that `adapter-mantine-v8`'s real build (which also declares `bundledPackages: ["@recursica/adapter-common"]`) doesn't actually hit the "bundled" branch either — both its call sites resolve `isExternal=true`, and its own `dist/index.d.ts` references `@recursica/adapter-common` types as ordinary external imports, not inlined. So `bundledPackages` wasn't doing anything there in practice. **Fix: drop `bundledPackages: ["@recursica/adapter-common"]` entirely, keep `rollupTypes: true`.** Verified the resulting `dist/index.d.ts` is shape-identical to `adapter-mantine-v8`'s real output (external `import { RecursicaXProps } from '@recursica/adapter-common'` references, not inlined). Separately found and fixed while investigating: `@recursica/storybook-template`'s own `package.json` pins `"@recursica/adapter-common": "^0.25.0"`, which doesn't overlap this repo's `^0.28.1`, so npm installed two physical copies (top-level `0.28.2` + a nested `0.25.1` under `storybook-template/node_modules`) — added `"overrides": { "@recursica/adapter-common": "$@recursica/adapter-common" }` to `package.json` to force a single deduped copy; didn't turn out to be the cause of the isExternal bug, but is worth keeping regardless to avoid two adapter-common versions silently diverging (e.g. in Storybook rendering).
- **`@recursica/official-release`'s postinstall script unconditionally overwrites `recursica.json`** the first time `npm install` runs and `recursica_variables_scoped.css` doesn't yet exist at the project root — confirmed by reading `node_modules/@recursica/official-release/scripts/postinstall.js`. It replaces a hand-authored `recursica.json` with the package's own default template, with no existence check on `recursica.json` specifically. Since the guard is keyed off `recursica_variables_scoped.css`, this only fires once per fresh clone — but it means **`recursica.json` customization must happen after the first `npm install`, not before** (or must simply be re-applied once, as done here). ~~The four large generated theme files are gitignored~~ **Superseded 2026-08-26 — see the entry below: all five files, including the four theme files, are checked into git.**
- **Added Husky + lint-staged + Prettier**, matching the monorepo's setup: `.husky/install.mjs` (near-verbatim copy — dropped its `turbo/no-undeclared-env-vars` eslint-disable comment, since there's no turbo eslint plugin here), `.husky/pre-commit` (verbatim copy), `scripts/lint-staged.config.js` (simplified for a single package — no Turborepo filter routing), `prettier.config.mjs` (empty, matching the monorepo's defaults), `.prettierignore` (excludes `dist`, `storybook-static`, and the postinstall-managed `recursica_*`/`recursica.json` files), and `format`/`prepare`/`precommit` scripts in `package.json`. `husky`/`lint-staged`/`prettier` devDependency versions pinned to match the monorepo's root `package.json`. Verified the hook actually installs: `npm install` runs `prepare` → `git config --get core.hooksPath` reports `.husky/_`.
- **`@types/node` had to be added as an explicit devDependency** — adding husky/lint-staged/prettier and re-running `npm install` changed npm's dependency-tree hoisting enough that `tsc --noEmit` started failing on every Node-builtin import in config files (`path`, `module`, `node:path`, `node:url`, `__dirname`) with `Cannot find module`/`Cannot find name` errors. The only `@types/node` in the tree was nested three levels deep under `@changesets/cli` → `@manypkg/find-root` (version `12.20.55`, far too old and not hoisted to a location `tsc` would find). This didn't surface in the first `check-types` run before these tools were added — hoisting nondeterminism, not a config regression. Added `"@types/node": "^24.0.0"` (matching CI's Node 24) explicitly rather than relying on incidental hoisting. Worth checking whether the genesis adapter has the same latent issue masked by the monorepo's much larger, differently-hoisted dependency tree — not verified here.
- **PR workflow runs `lint` explicitly** (the monorepo's root `pull-request.yml` doesn't, seemingly by omission rather than design, since `lint` is a defined script there too) — kept as a deliberate improvement for this single-package repo rather than a faithful copy.
- **`npm run storybook` doesn't show `@recursica/storybook-template`'s default token/theme demo stories** (Layer, Color, Dimensions, LayoutGrids, Type, Sizes, Font.\*, Opacities) — this is an **upstream packaging gap in `@recursica/storybook-template` itself**, not a mistake in this scaffold. `createMainConfig()`'s compiled output (`dist/main.js`) unconditionally appends a `stories` glob resolved relative to its own location — `<package-root>/stories/**/*.stories.*` — expecting a `stories/` folder to ship alongside `dist/`. But `packages/storybook-template/package.json`'s `"files"` array is `["dist", "templates"]`; it does not list `"stories"`, so that folder — which genuinely exists and is committed in the monorepo source — never makes it into the published npm tarball (verified with `npm pack @recursica/storybook-template --dry-run`: no `stories/` present). The genesis adapter (`mantine-adapter`) doesn't show this symptom only because, inside the monorepo, npm workspaces resolves `@recursica/storybook-template` via a symlink straight to the source package directory (confirmed: `node_modules/@recursica/storybook-template -> ../../packages/storybook-template`), which includes `stories/` on disk regardless of the npm "files" allowlist. Any adapter consuming the real published package — like this one — will not get those default stories until `@recursica/storybook-template`'s `"files"` array is fixed to include `"stories"` and a new version is published. **This needs to be fixed upstream in the `recursica` monorepo, not worked around here.**
